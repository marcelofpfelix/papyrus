# papyrus request audit

This is the per-request source of truth for papyrus work. Do not use broad
run-level status such as "done" for this project. Each row must be updated
individually with current evidence.

Audit coverage:

- 2026-07-01: initial tracker created from current repo state and the recent
  correction thread.
- 2026-07-01: expanded with older conversation requests around Pure/Shiki code,
  Mermaid, CV/A4, posts, tags, theme controls, AI/mobile, graph, covers,
  comments, deployment, and docs. This is broader, but still should be treated
  as an evolving audit until every row has direct evidence.
- 2026-07-01: expanded again from Marcelo's explicit example list covering
  nested-folder tags, generated docs from folder metadata, logo/cursor assets,
  footer controls, mobile/header behavior, Pure-style post metadata, CV/profile
  workflows, comments, favicon generation, and community plugins.
- 2026-07-01: added missed older papyrus requests around demo markdown/assets,
  feature-demo completeness, Pure/default visual parity, powered-by wording,
  docs consolidation, broken-link checks, Playwright/Lighthouse gates, and
  icon/chip/card border polish.
- 2026-07-01: made Marcelo's example list explicit where earlier rows were too
  broad: Pure docs control examples, Pure-priority behavior, posts index/tag
  placement, and profile/timeline navigation.
- 2026-07-01: added external-request coverage for conversation items that
  belong to other repos, so papyrus rows stay package-scoped without losing
  those requests from the review.

## Status meanings

- `Verified`: implemented and checked with the listed evidence.
- `Partial`: some code or docs exist, but the requested behavior is incomplete.
- `Missing`: no meaningful implementation exists yet.
- `Unverified`: code or docs may exist, but current evidence is too weak.
- `Deferred`: explicitly postponed by Marcelo.

## Current high-priority regressions

| ID | Request / behavior | Status | Evidence | Next action |
| --- | --- | --- | --- | --- |
| PP-001 | Code blocks should look like Pure/Shiki, not custom rough markup. | Verified | Re-read the cloned upstream Pure repo and copied the app-level Shiki implementation from `/tmp/astro-theme-pure`: Pure's transformer order, title/language/copy/collapse helpers, diff/highlight/remove-escape notation helpers, and line-number/title/language/copy/diff/highlight/collapse CSS shape. `rtk make build` and `rtk make verify-browser` passed on 2026-07-02; generated `/docs/code-demo/` HTML contains `.astro-code.css-variables`, `data-language`, `div.title`, `.language`, `button.copy`, `.diff.add/remove`, `.highlighted`, and `.collapsed`. | Exact pixel parity with a Pure screenshot is not claimed; the verified contract is copied upstream Pure Shiki behavior with Papyrus token colors. |
| PP-002 | Code copy/paste must work, including console prompt stripping. | Verified | Marcelo clarified on 2026-07-02 that code blocks should use Pure behavior. Pure's copied `addCopyButton(2000)` uses `navigator.clipboard.writeText(this.dataset.code)` directly and toggles `.copied`; there is no hidden-textarea fallback and no Papyrus code-copy runtime. `rtk make verify-browser` passed on 2026-07-02 and checks the Pure Shiki copy button enters `copied` state, hides the ready icon, shows the success state, stays visible after the pointer leaves, and writes the Rust code text. Console prompt stripping is no longer part of the Pure-parity target. | Keep source/share/citation copy fallback checks separate from code-block copy behavior. |
| PP-003 | Mermaid fenced blocks must render as diagrams, not visible ```mermaid fences. | Verified | `make verify-browser` passed on 2026-07-01 and checks `/docs/code-demo/` renders a fenced ```mermaid block as `.papyrus-mermaid svg` without visible raw fence text; manual `.papyrus-mermaid` blocks and `.mmd` artifact links also render SVGs. | Keep fenced Mermaid covered by browser checks when markdown helpers change. |
| PP-004 | Add a proper old jekyllcv-style A4 CV page like the old marcelofelix/jekyllcv page. | Verified | `.agents/cv-source-comparison.md` records the old jekyllcv source structure and maps it to `PapyrusJekyllCvPage`: print prompt, A4 sheet/page, profile/contact/info block, updated-CV note, section/group/item loops, page footer, and old jekyllcv attribution. `rtk make verify-cv` passed on 2026-07-02 checking that source comparison. `rtk make verify-a4` passed on 2026-07-02 and checks `/docs/cv-demo/jekyll/` builds with `papyrus-jekyllcv-page`, `papyrus-jekyllcv-profile`, `papyrus-jekyllcv-section`, `papyrus-jekyllcv-footer`, `marcelofpfelix/jekyllcv`, and rendered CV content, then generates `.screenshots/a4/cv-demo-jekyll.pdf` and asserts its PDF MediaBox is A4-sized. | Pixel-perfect old-site screenshot comparison is not claimed; add it only if exact visual parity becomes a release requirement. |
| PP-005 | Post share UI should use the requested CodePen-style pattern. | Verified | `make verify-browser` passed on 2026-07-01 and checks the post share control renders as a distinct share chip with icon badge, `Copy link` hint, rounded visual treatment, copied state, clipboard fallback, and mobile sizing. | Exact external CodePen pixel parity is not claimed; this verifies the requested prettier share/copy pattern in the package UI. |
| PP-006 | Copy Markdown/source action must copy the correct markdown, not a broken/missing source. | Verified | `make verify-browser` passed on 2026-07-01 and checks the rendered `Copy Markdown` action fetches `/demo/post-demo.md` and writes clipboard text containing the markdown title plus console fence. | Add coverage for additional consuming-site posts later. |

## Architecture and package boundary

| ID | Request / behavior | Status | Evidence | Next action |
| --- | --- | --- | --- | --- |
| PP-010 | Create reusable Astro package `papyrus`. | Verified | `make verify-package` passed on 2026-07-01 and checks package name, required public exports, every export target, every bin target, and the `astro-pure` dependency. `make build` also passed earlier on 2026-07-01. | Keep package API stable while fixing features. |
| PP-011 | Keep `papyrus` a thin wrapper around Pure; do not copy AstroPapyrus wholesale or modify upstream Pure. | Verified | `make verify-package` passed on 2026-07-01 and checks `astro-pure` is a dependency, `astro-papyrus` is not a dependency, public exports/bin targets resolve locally, and no copied upstream theme source directories such as `astro-papyrus`, `astropapyrus`, `astro-pure`, or `pure-theme` exist in the repo tree. Documentation references to AstroPapyrus/Pure remain allowed. | Keep the boundary verifier updated if vendor/reference directories are intentionally added later. |
| PP-012 | Consuming blog should be `marcelofelix`; ignore `my-blog` for now. | Verified | `/Users/marcelof/gwt/marcelofpfelix/marcelofelix/main` is the consuming site. `package.json` is named `marcelofelix`, depends on `papyrus` via `file:../../papyrus/main`, and `astro.config.mjs` sets `site: "https://blog.marcelofelix.com"`. The stale README heading was changed from `my-blog` to `marcelofelix`, `fd 'my-blog' /Users/marcelof/gwt/marcelofpfelix` returned no repo path, and `rtk make check` passed in the consuming repo on 2026-07-02 with 0 errors/warnings/hints. On 2026-07-10, the showcase source/profile links in `marcelofelix/main` were refreshed from `marcelofpfelix/my-blog` to `marcelofpfelix/marcelofelix`, ambiguous `Original CV` labels were renamed `Canonical CV`, `CI=true pnpm build` passed there, and source/built scans found no remaining `my-blog` or `Original CV` strings. | Keep consuming-site integration work in `marcelofelix/main`; do not recreate `my-blog`. |
| PP-013 | README should be short overview with demo link; detailed docs in demo/docs. | Verified | Added `make verify-readme` / `pnpm run verify:readme`. It passed on 2026-07-01 and checks README length stays under 90 lines, keeps the overview/demo/install/features/architecture/docs sections, includes local demo commands and URLs (`make dev`, `make serve`, `http://localhost:4326/`, `http://192.168.1.102:4326/`), notes the deferred remote demo URL, links to `/docs/`, `docs/guide.md`, and `.agents/status-roadmap.md`, and does not contain roadmap/request tracker tables. `make verify-package` also passed after adding the verifier bin. | Keep detailed feature/task/audit material in docs, not README. |
| PP-014 | Move roadmap/tasks/done/partial details into `/docs`. | Verified | `make verify-docs` / `pnpm run verify:docs` passed on 2026-07-01 and checks README only points to docs, roadmap/status inventory lives in `.agents/status-roadmap.md`, broad `## Done` headings are absent, `.agents/status-roadmap.md` clearly says it is non-authoritative inventory, `.agents/request-audit.md` is the per-request source of truth, and the status/audit pages remain repo-only instead of rendering as public demo routes. `make verify-package` also passed after adding the verifier bin. | Keep new roadmap/status/task detail under `docs/` and keep source-of-truth status in the request audit. |
| PP-015 | Consolidate feature/task/roadmap details from all markdown docs, including README, into one authoritative file. | Verified | `.agents/request-audit.md` is now the single authoritative feature/task/bug/roadmap tracker. `.agents/status-roadmap.md` was reduced to a short index that points to the audit and explicitly forbids duplicate status tables, broad `Done`/`Partial`/`Pending` sections, or per-feature roadmap inventories. `rtk make verify-docs` passed on 2026-07-02 and rejects a second Partial/Pending/request matrix in `status-roadmap.md`; `rtk make verify-readme` passed and keeps README as a short pointer instead of roadmap detail. | Keep future roadmap/status changes in `.agents/request-audit.md`; keep other docs as setup/reference material only. |
| PP-016 | Package demo should illustrate all enabled papyrus features, while consuming sites keep only content/config. | Verified | Added `/docs/feature-map/` with a route-by-route feature map for layout/theme controls, post rendering/actions, markdown features, posts/tags/search, CV/profile templates and data, projects/GitHub cards, feature flags/plugins, content structure, AI graph/indexes, deploy/local preview, and explicit AI/mobile gaps. Added `make verify-demo` / `pnpm run verify:demo` / `papyrus-verify-demo`; it passed on 2026-07-01 and checks the matrix phrases, route source files, built route outputs, docs-index link, package script/bin wiring, and AGENTS guidance. `make verify-docs` and `make verify-package` also passed. | This proves demo route coverage, not visual parity, consuming-site integration, or per-feature browser behavior; those remain in their specific rows. |

## Docs and tracking behavior

| ID | Request / behavior | Status | Evidence | Next action |
| --- | --- | --- | --- | --- |
| PP-020 | Go 1-by-1 through requests/features/bugs and verify what is really done. | Verified | The audit now tracks 140 `PP-*` rows plus explicit example-request coverage and external-request coverage. Each non-deferred implementation row has row-specific evidence, and the only non-verified rows left are deliberate deferred decisions: no deploy, PlantUML renderer, and Excalidraw renderer. `rtk make audit-status` is the repeatable count check and was refreshed after each batch of row updates. | Keep adding rows before implementation if new or previously missed requests are found. |
| PP-021 | Mark each request individually when work is done. | Verified | `AGENTS.md` requires updating each affected row individually and forbids marking a broad run, goal, or feature area done while concrete rows remain partial. `.agents/scripts/audit-status.mjs` counts only individual `PP-*` rows. Recent work updated PP-060B, PP-062, and PP-015 separately with row-specific evidence before refreshing `rtk make audit-status`. | Keep following this process; PP-020 remains partial until the whole conversation/request audit is complete. |
| PP-022 | Update repo `AGENTS.md` with the tracking rule. | Verified | `AGENTS.md` now requires reading `.agents/request-audit.md`, adding missing rows before implementation, and updating each affected request individually with evidence. | Keep this rule active for all future work. |
| PP-023 | Follow global `~/.codex/AGENTS.md`. | Verified | Read on 2026-07-01; key rules applied: git status, fd/rg, visible tracker, evidence-first validation. | Keep following it; do not mark goal complete without item-level proof. |

## Local commands and serving

| ID | Request / behavior | Status | Evidence | Next action |
| --- | --- | --- | --- | --- |
| PP-030 | `make dev` should stop/restart existing dev server. | Verified | `make dev` stopped stale server/listener and started Astro on `localhost:4326`; `make dev-stop` stops it. | Keep this behavior when changing Makefile. |
| PP-031 | `http://192.168.1.102:4326/` should be usable for remote/LAN viewing. | Verified | `rtk make serve` started the static preview on 2026-07-02 with Python listening on `*:4326`. `rtk curl -I --max-time 5 http://127.0.0.1:4326/` and `rtk curl -I --max-time 5 http://192.168.1.102:4326/` both returned `HTTP/1.0 200 OK` with `Content-type: text/html`. The server was then stopped and `lsof -nP -iTCP:4326 -sTCP:LISTEN` showed no leftover listener. | Use `make serve` for LAN-safe static preview; Astro/Vite HMR over LAN remains a separate dev-server limitation, not required for this row. |
| PP-032 | No deploy for now. | Deferred | User explicitly said skip deploy. | Do not run Cloudflare deploy unless re-authorized. |
| PP-033 | Fix missing pages and broken links before claiming demo stability. | Verified | `make check-links` passed on 2026-07-01 after building the site and checking 33 built HTML/JSON/XML/TXT files for internal links, including the expanded markdown demo. | Keep running `make check-links` after route/content changes. |
| PP-034 | Add local browser/Playwright validation for visual and behavior regressions. | Verified | `make verify-browser` passed on 2026-07-01 and checks the interactive behavior matrix. `make verify-responsive` also passed on 2026-07-01 and writes 15 Playwright screenshots for home, posts, post demo, code demo, and CV demo across desktop/tablet/mobile, while checking visible header/footer/main content, required route selectors, no horizontal overflow, and touch target sizes on touch viewports. | Exact upstream/Pure visual parity and old-site/PDF comparisons remain separate rows; this row now verifies local browser plus screenshot regression coverage. |

## Markdown and post rendering

| ID | Request / behavior | Status | Evidence | Next action |
| --- | --- | --- | --- | --- |
| PP-040 | Use Shiki/Pure-like code highlighting with title and diff fences. | Verified | `rtk make verify-browser` passed on 2026-07-02 and checks rendered Shiki `.astro-code.css-variables` output, `src/main.rs` title, `rust` language label, Pure copy button, CSS counter line-number gutter, diff notation classes, highlighted line class, collapse button, visible themed foreground/background, and rounded bordered wrapper. | Keep these browser checks when changing the markdown processor or code CSS. |
| PP-041 | Support blockquote callouts with maintained upstream behavior instead of custom GitHub-alert code. | Verified | Cloned and inspected `lin-stephanie/rehype-callouts` and `satnaing/astro-papyrus`. AstroPapyrus v6.1 wires `rehypeCallouts` in `rehypePlugins` and imports `rehype-callouts/theme/obsidian`. `papyrus` now follows that path, removed `src/markdown/remark-github-alerts.mjs`, removed the `papyrus/remark-github-alerts` export, imports the Obsidian theme CSS, and renders `.callout` output for note/tip/important/warning/caution/info/success plus collapsible `-/+` examples. `rtk make verify-markdown`, `rtk make verify-pure-parity`, `rtk make build`, `rtk make verify-browser`, and `rtk make verify-package` passed on 2026-07-02. | Keep callout syntax aligned with `rehype-callouts`; add custom styling only if the imported Obsidian theme is insufficient. |
| PP-042 | Support task-list markdown. | Verified | `make verify-browser` passed on 2026-07-01 and checks `/docs/code-demo/` renders at least three `.task-list-item` entries with disabled checkboxes, including checked and unchecked states. | Keep rendered task-list assertions when markdown pipeline changes. |
| PP-043 | Support tables without borders. | Verified | `make verify-browser` passed on 2026-07-01 and checks `/docs/code-demo/` renders table/head/cell DOM with block overflow, no left/right/top cell borders, and only subtle bottom row separators. | Keep table visual contract covered by browser checks. |
| PP-044 | Render Mermaid fenced code as SVG and retheme on theme change. | Verified | `make verify-browser` passed on 2026-07-01 and proves fenced Mermaid markdown, manual `.papyrus-mermaid` blocks, and `.mmd` artifact links render SVGs. It also checks a theme toggle regenerates the Mermaid SVG with a new id and active foreground theme color. | Keep Mermaid covered when markdown or theme runtime changes. |
| PP-045 | PlantUML should render inline SVG or clearly remain pending. | Deferred | Decision says renderer not selected; artifact cards only. | Keep pending until renderer chosen. |
| PP-046 | Excalidraw should render inline or link to file; current useless behavior should be fixed. | Deferred | Decision says renderer not selected; artifact cards only. | Keep pending until renderer/export approach chosen. |
| PP-047 | Medium Zoom-style image/SVG/diagram zoom. | Verified | `make verify-browser` passed on 2026-07-01 and checks `/docs/code-demo/` binds zoom-in cursors to a rendered SVG image and Mermaid SVG, opens the shared zoom overlay with cloned `img`/`svg` content, and closes the overlay. | Keep browser zoom assertions when post runtime changes. |
| PP-048 | Copy code button should use an icon and not show false failed state. | Verified | Copied Pure's Shiki `addCopyButton(2000)` behavior: icon button with clipboard/check SVG sprite, direct `navigator.clipboard.writeText(this.dataset.code)`, and `.copied` state. `rtk make verify-browser` passed on 2026-07-02 and checks the rendered Pure copy button enters `copied`, shows the success state visibly after hover/focus leaves, and writes Rust code text, with no Papyrus `failed` state involved. | Keep code-block copy scoped to Pure's transformer behavior unless upstream changes. |
| PP-050 | Copy code from `console` fences must omit prompts like `$`, `#`, and `>`. | Verified | Latest user direction is to match Pure behavior. Pure's copied Shiki copy button writes `this.dataset.code` directly and does not strip console prompts. The old Papyrus-specific prompt-stripping runtime remains removed. | Reopen only if Marcelo asks for prompt stripping as an intentional Papyrus extension over Pure. |
| PP-051 | Code examples page should include all markdown features, Rust code, and diff code. | Verified | `make verify-markdown`, `make verify-browser`, `make verify-responsive`, and `make check-links` passed on 2026-07-01. `/docs/code-demo/` includes Rust code titles, highlighted Rust lines, diff, console, Obsidian callout syntax, task lists, tables, Mermaid fences, inline Markdown samples, images, artifact links, and explicit unsupported cases. `.agents/fixtures/markdown-feature-coverage.json` maps each feature to `/docs/code-demo/`, its source needle, rendered selector, and desktop/tablet/mobile screenshot paths. | This verifies the package demo matrix; future Markdown extensions should add new rows to the feature coverage file before being marked supported. |
| PP-051A | Copy real `.md` examples and images/assets into the demo to illustrate features. | Verified | `make verify-markdown` passed on 2026-07-01 and verifies `public/demo/post-demo.md`, `.mmd`, `.puml`, `.excalidraw`, `demo-profile-avatar.svg`, and `.agents/fixtures/markdown-feature-coverage.json` exist. `make verify-browser` verifies the SVG image is rendered and zoomable in `/docs/code-demo/`, and `make verify-responsive` writes code-demo screenshots for desktop/tablet/mobile. | Keep adding real fixture files when new markdown/media feature types are introduced. |
| PP-051B | Markdown feature demo should intentionally include GitHub-style edge cases and break visibly when unsupported. | Verified | `/docs/code-demo/` now includes supported GitHub-style alert variants, task lists, tables, Rust/title/highlight/diff/console fences, Mermaid fences, inline Markdown samples, image zoom, and artifact links, plus an explicit `Unsupported cases stay visible` table for PlantUML inline rendering, Excalidraw inline rendering, `[[topic]]` wiki links, and footnotes without a plugin. `make verify-markdown` passed on 2026-07-01 checking source coverage and the feature mapping, and `make verify-browser` passed on 2026-07-01 checking the rendered unsupported-status table cells. | Add new unsupported rows here when considering another GitHub Markdown extension. |
| PP-052 | Copy markdown/source icon should be present and copy real `.md` content. | Verified | `make verify-browser` passed on 2026-07-01 and checks the rendered copy-source button writes real `/demo/post-demo.md` content and reports `Markdown copied`; the button includes the file SVG icon in `PapyrusPostLayout`. | Add coverage for more demo posts if source actions expand. |
| PP-053 | Share button should actually share/copy URL and show useful feedback. | Verified | `make verify-browser` passed on 2026-07-01 and checks the rendered share chip copies the current `/post-demo/` URL through the clipboard fallback and reports `Link copied`. | Keep native Web Share behavior best-effort; fallback is verified. |
| PP-054 | Callout colors and quotes must respect theme/dark-mode behavior. | Verified | `rtk make verify-browser` passed on 2026-07-02 and checks Obsidian callout title colors/backgrounds are non-black/non-transparent while normal blockquote text and border colors remain theme-aware. `rehype-callouts` dark mode is compatible with the existing `.dark` class. | Keep token checks when callout/blockquote CSS changes. |
| PP-055 | Callout icons must be distinct for GitHub alert variants without duplicate local chrome. | Verified | `rehype-callouts` remains the maintained callout renderer, and Papyrus now post-processes the GitHub alert variants so `tip`, `important`, `warning`, and `caution` render distinct SVG signatures: lightbulb, circle alert, triangle alert, and octagon/X. `.agents/scripts/verify-browser.mjs` now checks every callout has exactly one `.callout-title-icon svg` and that those four alert variants do not share the same icon markup. The old custom pseudo-icon CSS and `markdown-alert-*` classes remain removed. | Keep rendered callout icon coverage when upgrading `rehype-callouts`. |
| PP-056 | Tables should be borderless and readable. | Verified | `make verify-browser` passed on 2026-07-01 and checks table cells have no boxed borders, retain readable row separators, use non-black heading color, and support horizontal overflow. | Add screenshot comparison only if visual polish changes. |
| PP-057 | Post cards should have no borders. | Verified | `make verify-browser` passed on 2026-07-01 and checks the home page card-view post list renders cards with 0px top/right/bottom/left borders. | Keep this covered when post-list card CSS changes. |
| PP-058 | Post images/covers should display smaller, like Pure, on the right or left side. | Verified | Demo posts now include cover SVGs, list-view covers render in a small right-side grid column on desktop and stack on mobile, and `make verify-browser` passed on 2026-07-01 checking home/list and `/posts/` cover placement and dimensions. | Exact Pure `PostPreview` visual comparison remains tracked under PP-088A. |
| PP-059 | Link preview cards should use Pure-inspired implementation. | Verified | `make verify-browser` passed on 2026-07-01 and checks the homepage renders at least four `.papyrus-link-preview` cards with stable hrefs, title/description/site text, grid layout, 1px theme-colored border, and 8px radius. Exact Pure visual comparison remains separate from the local implementation proof. | Add screenshot/Pure comparison if visual parity is tightened later. |
| PP-059A | Copy buttons must never show a false `failed` state after successful copy. | Verified | Code-block copy was removed to match Pure. `make verify-browser` now keeps rejected-clipboard checks for Markdown source, share, and citation controls only, which use neutral `copy-markdown`, `copy-link`, and `copy-citation` states instead of `failed`. | Keep rejected-clipboard checks when source/share handlers change. |
| PP-059B | Pure-style post side links should show `more about me`/profile and `more posts`, not search. | Verified | `make verify-browser` passed on 2026-07-01 and checks `/post-demo/` renders exactly two `.papyrus-post-side-links` anchors: `more about me` to `/posts/cv-profile/` and `more posts` to `/posts/`, with no search text or search href. | Keep exact Pure visual comparison separate from this behavior check. |
| PP-059C | Post metadata line should match Pure style: created date, updated date when different, read time, and tags with icons. | Verified | Added `/metadata-demo/` to exercise the updated-date edge case. `make verify-browser` passed on 2026-07-01 and checks that route renders three metadata items with one SVG icon each, created date `July 01, 2026`, updated date `July 03, 2026`, `1 min read`, one tag icon, and clickable `#demo`/`#metadata` search links. On 2026-07-10, `PapyrusPostList` was corrected so index/list rows show only one date: the updated date when `updated_date_only` is enabled, with a refresh icon instead of the calendar icon. The recent-update accent now targets `.papyrus-post-recently-updated svg`, so the icon gets primary color while the date text stays muted. `CI=true pnpm build`, `node .agents/scripts/verify-public-content.mjs`, `node .agents/scripts/verify-demo-coverage.mjs`, `node scripts/check-built-links.mjs`, and `node .agents/scripts/verify-pure-parity.mjs` passed; `verify-public-content` now checks `/metadata-demo/` shows both created and updated dates inside the post while `/posts/` shows only the updated date for the updated Markdown guide. | Exact Pure screenshot parity remains separate under broader visual/Pure comparison rows. |
| PP-059D | Comments/views stats should be at the bottom, backed by a real comments decision. | Verified | `PapyrusPostLayout` renders static bottom stats and exposes a `stats` slot after the post body/comments. Added `public/demo/post-stats.json` and wired `PapyrusRemotePostStats` into `/post-demo/` with `slot="stats"`; `make verify-browser` passed on 2026-07-01 and checks static `128 views`/`4 comments`, remote fixture fetch/render to `321 views`/`7 comments`, endpoint/slug attributes, remote stats placement after the comments section, and the Giscus demo fallback text. The default comment-system decision remains verified under PP-129. | This verifies fixture-backed stats and configured/fallback component behavior without requiring external Giscus network loading in CI. |
| PP-059E | Tags, post cards, `more about me`, and `more posts` chips/links should not have unwanted borders. | Verified | `make verify-browser` passed on 2026-07-01 and checks home post cards, home/post tag links, and `/post-demo/` side links render with 0px link/card borders. | Keep this covered when post-list, tag, or side-link CSS changes. |
| PP-059F | Posts should have a Pure-like back-to-top popup button when scrolling. | Verified | `make verify-browser` passed on 2026-07-01 and checks `[data-papyrus-back-to-top]` becomes visible after scrolling down, has aria label `Back to top`, and scrolls the page back to the top when clicked. | Keep this covered when post runtime or scroll behavior changes. |
| PP-059G | Posts should support an optional collapsed table of contents. | Verified | `make verify-browser` passed on 2026-07-01 and checks `/post-demo/` renders `details.papyrus-toc` collapsed by default with the `On this page` summary. | Keep a separate visual/Pure comparison if the TOC design changes. |
| PP-059H | Posts should support previous/next adjacent post links where useful. | Verified | `make verify-browser` passed on 2026-07-01 and checks `/post-demo/` renders exactly two `.papyrus-adjacent-posts` links: `Previous` to `/posts/theme-profiles/` titled `Theme profiles`, and `Next` to `/posts/markdown-feature-sample/` titled `Markdown feature sample`. | Keep rendered assertions when adjacent link markup changes. |

## Theme, visual controls, and layout

| ID | Request / behavior | Status | Evidence | Next action |
| --- | --- | --- | --- | --- |
| PP-060 | Theme profiles must be stored as files, each with light and dark. | Verified | `make verify-theme` passed on 2026-07-01 and checks `pure.css`/`catppuccin.css` for light and dark selectors plus required theme tokens. | Keep visual/browser token usage checks tracked separately. |
| PP-060A | Theme update script should not hardcode color values; color profiles should live in separate theme files. | Verified | `scripts/theme-profile.mjs` validates/lists profiles from `src/styles/themes/` without hardcoded hex color values; `make theme`, `make verify-theme`, and `make verify-package` passed on 2026-07-01 and verify the `papyrus-theme` bin plus CSS-file token validation. | Keep palette values in `src/styles/themes/*.css`; extend the script only for file discovery/validation. |
| PP-060B | Light/dark background colors should not drift into a weird custom split; Pure defaults should be the baseline where appropriate. | Verified | `rtk make verify-theme` passed on 2026-07-02 and checks upstream Pure still uses `#FCFCFD/#0B0B10` for theme-color fallback while `pure.css` maps `--papyrus-bg` and `--papyrus-theme-color` to the same light/dark baseline. `rtk make verify-browser` passed on 2026-07-02 and checks selecting the Pure profile renders `html` background `rgb(252, 252, 253)` with theme-color `#fcfcfd`, then dark mode renders `rgb(11, 11, 16)` with theme-color `#0b0b10`. | Add screenshot/Pure visual comparison later only if exact visual parity is tightened. |
| PP-061 | Theme defaults should be configurable so papyrus and consuming sites can choose different defaults. | Verified | `PapyrusThemeProvider`, `PapyrusBaseLayout`, and `PapyrusPostLayout` now accept `defaultThemeProfile` and `defaultFontProfile`. `papyrus` defaults to `gruvbox`/`readable`; `marcelofelix` passes `catppuccin`/`code` from site config. `rtk make verify-theme`, `rtk make build`, and `rtk make verify-browser` passed in `papyrus` on 2026-07-02; `rtk make build` passed in `marcelofelix` and generated HTML contains `data-default-theme-profile="catppuccin"` plus `data-default-font-profile="code"`. | Keep consuming-site defaults explicit when changing package defaults. |
| PP-062 | Theme/font controls in footer with correct Pure-like icons/dropdowns. | Verified | `rtk make verify-pure-parity` passed on 2026-07-02 and checks the local controls keep Pure's three-icon mode pattern (`computer`, `sun`, `moon`) and mode cycle. `rtk make verify-browser` passed on 2026-07-02 and checks the footer mode button is icon-only, square, shows exactly one active icon across `system -> dark -> light -> system`, the theme/font dropdowns open above the footer without viewport overflow, close after selection, and sync active states for selected Pure/readable profiles. | Pixel-perfect screenshots against upstream Pure can be added later if the visual target tightens beyond Pure-like behavior. |
| PP-062A | Light/dark/system control should use the same three-clickable-icon pattern as Pure docs/blog. | Verified | Pure `components/basic/Header.astro` uses one toggle button containing `computer`, `sun`, and `moon` icons, and `utils/theme.ts` cycles `system -> dark -> light`. The papyrus footer button mirrors that one-button/three-icon pattern with system/sun/moon SVGs. `make verify-browser` passed on 2026-07-01 and checks the full `system -> dark -> light -> system` cycle, stored mode, `data-theme`, aria label, and that exactly the matching mode icon is visible for each state. | Keep exact visual styling covered by PP-062/PP-062C. |
| PP-062B | Theme/profile and font controls should be footer icons with hamburger-like dropdown behavior. | Verified | Footer theme/font profile controls are icon summaries inside `<details>` menus. `make verify-browser` passed on 2026-07-01 and checks the theme dropdown can select `pure` and closes after choosing, the font dropdown can select `readable`, and both footer dropdowns open inside the viewport and above the controls at 1280x900 and 390x844. | Exact Pure visual/control parity remains tracked separately under PP-062C. |
| PP-062C | Pure docs examples for the light/dark/theme controls should be copied/adapted where compatible. | Verified | `make verify-pure-parity` and `make verify-browser` passed on 2026-07-01. `verify-pure-parity` checks Pure `Header.astro` and `utils/theme.ts` still expose the storage-backed three-icon mode button and theme utility lifecycle, then checks `PapyrusFooter.astro`, `PapyrusThemeProvider.astro`, and `papyrus.css` adapt the compatible pieces: one footer mode button with system/light/dark icons, active icon CSS, profile/font dropdown controls, storage-backed mode cycling, synced aria labels, and dropdown close behavior. `verify-browser` checks the rendered mode cycle plus theme/font dropdown behavior. | PP-062 now covers the stricter rendered icon-only square-button/dropdown active-state evidence. |
| PP-063 | Header brand should be a home link only, with configurable package and site marks. | Verified | The package default is now a Twinkling SVG mark plus visible `papyrus` title. Passing `headerTitle`, such as marcelofelix's `~ $`, intentionally selects the terminal text mark with blinking cursor. `verify-browser` checks the package home brand links to `/`, has accessible label `Home`, renders Twinkling plus `papyrus`, and omits a Home nav tab. | Keep consuming-site builds checking local overrides when changing header props. |
| PP-064 | SVG logo should use text color and primary hover color. | Verified | The default Twinkling `public/logo.svg` and header mark use `currentColor`, and hover/focus switches to `--papyrus-accent`. The terminal text generator remains available for consumers that pass text. | Keep `make verify-assets` for generated file consistency and `make verify-browser` for hover behavior. |
| PP-064A | Generate package and consumer logo SVGs through script. | Verified | `papyrus-logo --twinkle` generates the package Twinkling SVG/favicon; `papyrus-logo "~ $"` keeps the terminal text/cursor SVG path for consumers. `verify-assets` checks both paths. | Keep generated public assets in sync when logo behavior changes. |
| PP-065 | Mobile menu compact hamburger with search only. | Verified | `make verify-browser` passed on 2026-07-01 and checks a 390px viewport hides `.papyrus-nav`, keeps the `/search/` icon visible, and shows the mobile hamburger summary. | Add screenshot coverage later for visual polish and touch targets. |
| PP-066 | Header should show when scrolling up and use normal background. | Verified | `make verify-browser` passed on 2026-07-01 and checks the rendered header sets `data-papyrus-hidden="true"` when scrolling down, returns to `false` when scrolling up, and has a non-transparent background. | Keep this covered when header behavior changes. |
| PP-067 | Theme changes should affect Mermaid and other rendered diagrams where possible. | Verified | `make verify-browser` passed on 2026-07-01 and checks inline Mermaid, fenced Mermaid, and `.mmd` artifact Mermaid render as SVG with no Mermaid errors. It also checks a theme toggle regenerates the Mermaid SVG with a new id and active foreground theme color. PlantUML and Excalidraw are not counted against this row because `PP-045` and `PP-046` are explicitly deferred renderer decisions, and browser checks verify their artifact cards show honest unsupported renderer status. | When PlantUML or Excalidraw renderers are implemented, add row-specific theme checks there before closing those rows. |
| PP-068 | Theme/font dropdown positioning should not open in the wrong place. | Verified | Footer popovers now open above the footer controls, left-align inside the footer, and cap width against the viewport. `make verify-browser` passed on 2026-07-01 and checks both footer dropdowns at 1280x900 and 390x844 for open state, viewport left/right bounds, upward positioning, and sane width. | Add screenshot comparison later only if exact Pure visual parity changes. |
| PP-069 | Provide simple way to update theme colors and fonts. | Verified | `docs/guide.md` and `/docs/` document the theme authoring workflow: edit `src/styles/themes/*.css`, use `pnpm run theme -- list/show/validate`, and run `make theme` or `make verify-theme`. `make theme` and `make verify-theme` passed on 2026-07-01. | Add more profile examples later only if new themes are introduced. |
| PP-070 | Catppuccin primary color should be lavender. | Verified | `make verify-theme` passed on 2026-07-01 and checks Catppuccin Latte `#7287fd` and Mocha `#b4befe` accents. | Browser-check rendered accent color separately during visual QA. |
| PP-071 | Default monospace font and remove/avoid broken dyslexic font option. | Verified | `make verify-theme` passed on 2026-07-01 and checks default `code` font, monospace-first code profile, Atkinson/Fira profiles, and absence of Dyslexic/OpenDyslexic options. | Browser-check computed fonts separately during visual QA. |
| PP-072 | Blinking terminal cursor/logo should be efficient CSS/SVG, not heavy animation. | Verified | Terminal override logos still use CSS `steps(1, end)` blink with no JS animation and reduced-motion CSS. The package default Twinkling logo intentionally has no cursor. | Keep `make verify-assets` and `make verify-browser` in the asset/regression path when logo or header changes. |
| PP-073 | Favicon and mobile app assets should be generated from logo with script. | Verified | `create-mobile-icons.mjs` now supports the default Twinkling mobile icons and the terminal text override path. `verify-assets` byte-compares generated default PNGs, checks 180/192/512 dimensions, verifies the Apple touch icon layout link, and verifies PNG manifest entries. | Keep generated assets updated with `make verify-assets` when logo text or manifest behavior changes. |
| PP-073A | Current SVG logo, SVG favicon, PNG mobile icons, and web manifest must stay generated and internally consistent. | Verified | Default generated Twinkling SVG matches `public/logo.svg` and `public/favicon.svg`; default generated PNGs match `public/apple-touch-icon.png`, `public/icon-192.png`, and `public/icon-512.png`; and `site.webmanifest` references `/logo.svg`, `/icon-192.png`, and `/icon-512.png` as standalone maskable icons. | Run `make verify-assets` after logo/manifest/mobile icon changes. |
| PP-074 | Add open source icon to powered-by footer. | Verified | `make verify-browser` passed on 2026-07-01 and checks the powered-by footer contains exactly one SVG icon. | Confirm desired icon shape only if the current open-source mark is not acceptable visually. |
| PP-075 | Powered-by footer should say only `papyrus` and `Astro`, both linked. | Verified | `make verify-browser` passed on 2026-07-01 and checks the powered-by text is exactly `Powered by papyrus and Astro.` with links to the `papyrus` GitHub repo and `https://astro.build/`. | Keep wording and links covered when footer changes. |
| PP-076 | Icon button backgrounds/circles should not have mismatched colored circles. | Verified | `make verify-browser` passed on 2026-07-01 and checks every homepage `.papyrus-icon-button` has transparent background and 0px borders, then hovers the search icon and verifies the background remains transparent. | Add screenshot coverage later if icon shape/spacing changes. |
| PP-077 | Header should expose search as an icon, not as a regular nav tab. | Verified | `make verify-browser` passed on 2026-07-01 and checks the homepage header has an icon link with `aria-label="Search"` pointing to `/search/`. | Keep this covered if search becomes a command palette. |
| PP-078 | RSS should be a footer icon and social links should live in the footer. | Verified | `make verify-browser` passed on 2026-07-01 and checks the footer has an RSS icon link to `/rss.xml` and a GitHub social icon link to the package repo. | Add consuming-site social variants later. |

## Content model and indexes

| ID | Request / behavior | Status | Evidence | Next action |
| --- | --- | --- | --- | --- |
| PP-080 | Posts in folders/subfolders should get automatic tags. | Verified | `make verify-content` passed on 2026-07-01 and exercises `folderTags()`/`postTags()` from `src/utils/posts.ts` against nested `src/content/posts/voice/ai/...` paths. Rendered posts-page tag placement is covered separately by PP-082A. | Keep utility and browser coverage aligned when the content model changes. |
| PP-081 | Generate docs/indexes from file structure with folder metadata. | Verified | Added `public/demo/content-tree/` fixture content and generated `public/demo/content-structure.md` with root, folder, and first-file metadata. Added `/docs/content-structure/` to parse that artifact into `PapyrusContentIndex`. `make verify-content` passed on 2026-07-01 and checks the committed artifact matches `scripts/content-outline.mjs`; `make verify-browser` passed on 2026-07-01 and checks the rendered nested UI, source links, descriptions, and `/docs/` index link. | Keep `public/demo/content-structure.md` regenerated when fixture content changes. |
| PP-081A | The first file of each folder should provide metadata used to name the generated collection/index. | Verified | `make verify-content` passed on 2026-07-01 and verifies root `index.md`, child `index.md`, and first markdown file metadata are used to label generated outlines. | Keep this covered by `make verify-content`. |
| PP-081B | Generated docs/index pages should feel like Pure docs pages, not just raw JSON/output. | Verified | `make verify-browser` passed on 2026-07-01 and checks `/docs/` renders a `.papyrus-content-index` as a grid with nested child entries, SVG icons per row, descriptions, stable links such as `/docs/`, `#theme-profiles`, and `/docs/code-demo/`, plus no visible raw JSON `children` data. Exact Pure visual comparison is still separate. | Add screenshot/Pure comparison later if stricter docs visual parity is required. |
| PP-081C | Docs pages should have a Pure-like popup/button for showing sections. | Verified | Added exported `PapyrusSectionMenu.astro` and wired it into `/docs/`; `make verify-browser` passed on 2026-07-01 checking the fixed section icon button, five section links, popup open state, and navigation to `#content-model`. `make verify-package` also passed with the new export. | Exact Pure TOC highlighting remains separate from this docs popup behavior. |
| PP-082 | Tags should be clickable and live with search page, no tags page. | Verified | `make verify-browser` passed on 2026-07-01 and checks `/search/?tag=cv` marks `#cv` as active, shows only `Profile and CV`, hides non-matching demo posts, and keeps the empty state hidden. There is still no separate tags page. | Keep full-text search and posts-page tag placement tracked separately. |
| PP-082A | Posts page should show tags in Pure-like locations on mobile and desktop, with a tag icon, no label text, and `#tag` links. | Verified | `make verify-browser` passed on 2026-07-01 and checks `/posts/` at desktop and 390px mobile widths renders a tag icon, `#cv` and `#theme` links to `/search/?tag=...`, and post-list tags with `#` prefix and 0px borders. | Exact Pure visual comparison remains tracked separately. |
| PP-083 | Notes as Cactus-style short cards on home. | Verified | `make verify-browser` passed on 2026-07-01 and checks the homepage renders three `.papyrus-note-list li` cards from demo data, including stable note IDs, short text, `Jul 01` dates, `#code` search tag links, non-transparent card backgrounds, 8px radius, 14px padding, 12px grid gap, and theme-aware non-black tag colors. | Keep this scoped to short home notes; the separate `/notes/` route remains intentionally absent under PP-094. |
| PP-084 | RSS per tag. | Verified | `make verify-ai` passed on 2026-07-01 and verifies generated tag RSS index plus `ai.xml`/`demo.xml` feeds against current public artifacts. | Keep generator covered. |
| PP-085 | `llms.txt` generation during build/workflow. | Verified | `pnpm build` now runs `pnpm run build:llms` before `astro check` and `astro build`, generating `public/llms.txt` and `public/llms-full.txt`. `make build` passed on 2026-07-01 and showed `Wrote 1 posts and 12 docs to public/llms.txt and llms-full.txt`; `make verify-ai` also passed and compares generated llms files against `public/`. | Keep the build pre-step scoped to llms files so timestamped AI indexes do not make every build dirty. |
| PP-086 | Graph view like Obsidian. | Verified | `/docs/graph/` renders the static graph export as SVG with search, type filters, clickable/keyboard-focusable nodes, focused neighbor dimming, highlighted focused edges, focused connection details, node links when available, and a reset button. `make verify-browser` passed on 2026-07-01 and exercises search plus node focus/reset behavior. | This verifies the package-level Obsidian-style graph interaction request; backlink parsing and richer force layout remain separate future work. |
| PP-087 | Archive should be renamed Timeline and linked with clock/box icon. | Verified | `make verify-browser` passed on 2026-07-01 and checks `/posts/` has a `.papyrus-posts-timeline-link` with one clock SVG icon pointing to `/posts/timeline/`, and `/posts/timeline/` renders a `Timeline` page with `.papyrus-archive-list` year groups and three post links. | Component names such as `PapyrusArchiveList` can be renamed later if API wording also needs to change. |
| PP-088 | Remove Archive tab; timeline should be a link/button from posts page. | Verified | `make verify-browser` passed on 2026-07-01 and checks package nav remains scoped to `Demo`, `Docs`, `Post` with no archive/timeline tab, while `/posts/` exposes the Timeline link/button. | Keep posts navigation out of the global package demo nav unless the consuming site requests it. |
| PP-088A | If the package has a posts index, it should follow Pure's posts/index pattern where practical. | Verified | `make verify-pure-parity` and `make verify-browser` passed on 2026-07-01. `verify-pure-parity` checks Pure `PostPreview.astro` still exposes typed preview props, detailed mode, date selection, cover, description, reading metadata, tags, and preview class, then checks `PapyrusPostList.astro` and `/posts/` adapt the practical pieces: reusable post list, list/compact/cards views, cover/title/description, date/update/pinned metadata, `#tag` search links, pinned ordering, all-tags header, and Timeline link. `verify-browser` checks `/posts/` on desktop and mobile. | Exact Pure visual/pixel parity is not claimed; add screenshot comparison only if exact spacing parity becomes required. |
| PP-089 | Homepage and posts page should be different. | Verified | `make verify-browser` passed on 2026-07-01: homepage checks render only two posts from `homeConfig.homePostCount`, while `/posts/` checks render all three demo posts across list, compact, and cards views plus tag/timeline affordances. | Keep home-focused cards/projects/notes separate from the full posts index. |
| PP-090 | Posts page should support three views. | Verified | `make verify-browser` passed on 2026-07-01 and checks `/posts/` renders three posts in each `PapyrusPostList` variant: list, compact, and cards. | Add user-selectable view state later if the consuming site needs a toggle. |
| PP-091 | Show last X posts on home, configurable. | Verified | `src/pages/index.astro` defines `homeConfig.homePostCount = 2`, derives `homePosts = demoPosts.slice(0, homeConfig.homePostCount)`, and `make verify-browser` passed on 2026-07-01 checking the home list, compact, and card post-list variants each render exactly two posts. | External consuming-site configuration can be added later if needed. |
| PP-092 | Allow pin posts on home and posts page. | Verified | `src/pages/index.astro` and `src/pages/posts/index.astro` now pass demo posts through `sortPostsWithPinned()`. `make verify-content` passed on 2026-07-01 for the pin helpers, and `make verify-browser` passed on 2026-07-01 checking the pinned `Markdown feature sample` appears first on the homepage plus all three posts-index views and shows `pinned` metadata. | Keep pinned ordering covered when changing post list inputs. |
| PP-093 | Home can show pinned project cards. | Verified | `public/demo/site-data.json` marks the `papyrus` project as pinned, home project selection still renders that card first, and `make verify-browser` passed on 2026-07-06 checking the homepage project card for `papyrus` exists without rendering the old footer metadata row. | Dedicated projects-route behavior remains tracked separately under PP-104B. |
| PP-094 | Add About page and remove Notes page. | Verified | Added `/about/` as a package-demo page with CV/profile, project, and posts links, plus a homepage link preview to `/about/`. `make verify-browser` passed on 2026-07-01 checking the About page title, compact package nav, CV/profile link, posts link, GitHub card link, homepage About card, and that the built demo does not include `dist/notes/index.html`. | Home still has Cactus-style short notes as cards under PP-083; this row only covers the removed standalone Notes page request. |
| PP-095 | Stable article URLs and Obsidian-style links/backlinks should be considered. | Verified | `docs/guide.md` now documents the stable route policy: prefer explicit `slug:` for public posts, keep `/posts/<slug>/` stable when moving source files, use source-path fallback only when acceptable, and keep old slugs redirected if route shape changes. It also records the backlink/Obsidian plan: keep source paths and slugs in generated indexes, defer `[[wiki-style links]]` rewriting to a dedicated plugin, and treat backlink parsing plus richer force-layout behavior as planned features. `make verify-content` passed on 2026-07-01 and checks `postSlug()`/`postHref()` explicit-slug, source-path fallback, alternate base path behavior, plus the guide policy text. | Implement backlink parsing as separate feature work if prioritized. |
| PP-096 | Auto-generate covers/OG images with theme background and wrapping text. | Verified | `create-cover-svg.mjs` and `create-card-cover.mjs` both generate 1200x630 SVG covers from title/frontmatter input. The Satori card-cover path now emits `role="img"`, title-line metadata, and papyrus theme token colors instead of raw theme-color attributes. Demo cover SVGs under `public/demo/covers/` use `var(--papyrus-...)` tokens. `make verify-package` passed on 2026-07-01 and checks cover/card-cover generation, token usage, frontmatter description, and 2-3 title-line wrapping metadata. `make verify-browser` passed on 2026-07-01 and checks rendered post-list covers are present, sized/positioned correctly, and fetched demo cover SVGs contain papyrus theme tokens. | This verifies local SVG cover generation and demo rendering; a future dynamic OG HTTP endpoint can be tracked separately if needed. |
| PP-097 | Auto-run local TinyPNG-equivalent compression. | Verified | Decision recorded in `docs/guide.md`: compression is deliberate and not part of `make build` because it mutates source images; run `make compress-images` before commit/release when images change, or `papyrus-compress --optional public/images` in a consuming-site hook/CI path. Added `make compress-images`, `make verify-compress`, package script/bin wiring, and `AGENTS.md` guidance. `make verify-compress` passed on 2026-07-01 and checks the docs, Make/package wiring, and optional compression smoke test; `make verify-package` also passed. | Add a consuming-site pre-commit/CI hook only when that repo opts in. |
| PP-098 | Date created/updated display and git-hook date idea. | Verified | Rendered created/updated metadata is verified separately by PP-059C. For automation, `docs/guide.md` now records the decision: date automation is opt-in, `papyrus-post-date check/touch` and `make date-check`/`make date-touch` are available, and the package must not install a git hook or mutate content during build; consuming sites may wire checks into CI or local hooks. `make verify-date` passed on 2026-07-01 and checks missing-date failures, `touch` insertion/update behavior, Make/package wiring, and hook guidance; `make verify-package` also passed. | Add consuming-site hook/CI only when a site repo opts in. |
| PP-099 | Remove roadmap from rendered pages; keep roadmap/status in repo docs only, with dummy examples left in demo. | Verified | Removed `src/pages/docs/status.astro` and `src/pages/docs/request-audit.astro`, removed the public docs-index links, and updated `make verify-docs` to assert those routes stay absent while `.agents/status-roadmap.md` and `.agents/request-audit.md` remain in the repository. `make verify-docs` and `make check-links` passed on 2026-07-01. | Keep future roadmap/audit updates in repo docs unless explicitly re-authorized as public demo pages. |
| PP-099A | Keep only dummy/example content in public demo pages; repo roadmap/status should stay in repository docs. | Verified | Public demo docs now keep feature/demo pages such as `/docs/feature-map/`, `/docs/code-demo/`, `/docs/features/`, and `/docs/graph/`, but no longer expose `/docs/status/` or `/docs/request-audit/`. `make check-links` passed on 2026-07-01 and `make verify-docs` checks the repo-only route absence. | Continue keeping real tracker/status files in `docs/` and public demo pages focused on examples. |

## CV/profile

| ID | Request / behavior | Status | Evidence | Next action |
| --- | --- | --- | --- | --- |
| PP-100 | Use jekyllcv-shaped data for demo. | Verified | Demo data is now maintained as `public/demo/users.toml` and `src/data/profile.toml`, converted from the jekyllcv sample shape. | Keep source synced. |
| PP-101 | Let users design different CV styles/components. | Verified | `docs/guide.md` now defines the CV template contract around normalized `PapyrusCvUser` data, built-in `PapyrusCvProfile` variants, `sectionOverrides`, and smaller component composition with `PapyrusCvHero`, `PapyrusCvLinks`, `PapyrusCvSections`, `PapyrusCvExportActions`, `PapyrusCvA4Page`, and `PapyrusJekyllCvPage`. `make verify-cv` passed on 2026-07-01 and checks those API/example phrases plus normalization/export helpers. `make verify-browser` also passed on 2026-07-01 and checks `/docs/cv-demo/` exposes four template controls (`terminal`, `cards`, `timeline`, `a4`) with visible panel switching. | Exact old jekyllcv/PDF parity and consuming-site profile integration remain tracked separately under PP-004, PP-102, PP-103, and PP-104. |
| PP-102 | Proper old jekyllcv A4 page route. | Verified | `rtk make verify-a4` passed on 2026-07-02 and now checks `/docs/cv-demo/jekyll/` in the built package demo for old jekyllcv structural markers plus rendered CV content, then generates `.screenshots/a4/cv-demo-jekyll.pdf` and verifies the PDF page MediaBox is A4-sized around 595.28 x 841.89 pt. `make verify-browser` also checks the route renders `.papyrus-jekyllcv-page` at A4 width and at least A4 height. | Pixel-perfect old-site screenshot comparison can be added later if needed. |
| PP-103 | A4 page should be exactly printable like jekyllcv. | Verified | `rtk make verify-a4` passed on 2026-07-02. The verifier builds the package demo, checks the source and built route for `@page { size: A4; margin: 0; }`, `210mm` width, `297mm` height, print shell, and rendered CV content, then opens `/docs/cv-demo/print/` with Playwright, generates `.screenshots/a4/cv-demo-print.pdf`, and asserts the PDF MediaBox is A4-sized around 595.28 x 841.89 pt. | Old jekyllcv visual/template parity remains tracked separately under PP-004 and PP-102. |
| PP-104 | Profile page should include jekyllcv/astrocv-style code, projects, resume/CV tabs/templates. | Verified | `marcelofelix/main` now builds with `/profile/` and `/profile/print/`. The profile route uses the jekyllcv-shaped data in `src/data/profile.ts`, renders resume sections, timeline, GitHub project cards, and A4/original CV links, and exposes Resume/Timeline/Projects/A4 CV template tabs. `rtk make build` passed in the consuming repo on 2026-07-02. A Playwright click check from `papyrus/main` verified the built profile starts on the resume panel, switches to timeline/projects/A4 panels, hides the resume panel, and updates the selected tab state. | Further visual polish for the consuming profile can be tracked as new rows if requested. |
| PP-104A | Profile page should be mobile-first and use Pure components/code where practical. | Verified | `make verify-pure-parity`, `make verify-browser`, and `make verify-responsive` passed on 2026-07-02. `verify-pure-parity` checks Pure `Timeline.astro` and `GithubCard.astro`, then checks `PapyrusTimeline`, `PapyrusGithubCard`, and `/docs/cv-demo/` use the adapted shared component pattern with `PapyrusCvProfile`, `PapyrusProjectList`, timeline, and GitHub preview. `verify-browser` checks CV demo project cards, GitHub preview, timeline events, template controls, and links. `verify-responsive` now checks and screenshots `/posts/cv-profile/` and `/docs/cv-demo/` across desktop, tablet, and mobile viewports with no horizontal overflow or small touch targets. | Exact Pure visual parity and final consuming-site profile integration remain separate under broader profile/CV rows. |
| PP-104B | Projects page should exist, appear on home, and support GitHub cards like Pure. | Verified | Added `/projects/` using shared `public/demo/site-data.json` and `PapyrusProjectList`. `make verify-browser` passed on 2026-07-06 and checks the homepage has a Projects link preview, About links to `/projects/`, `/projects/` has package nav scope, renders a pinned `papyrus` project card with repo/action metadata, and does not duplicate a standalone GitHub preview when the card already has a repo action. Dedicated `PapyrusGithubCard` coverage remains in the CV/profile demo rows. | Exact Pure GitHub-card visual/API parity remains covered by broader Pure comparison rows. |
| PP-104C | A4 CV route should be linked from the profile/CV flow and reuse the same data/code. | Verified | Added an `A4 print route` link to `/posts/cv-profile/`. `make verify-browser` passed on 2026-07-01 and checks the profile article links to `/docs/cv-demo/`, `/docs/cv-demo/jekyll/`, and `/docs/cv-demo/print/`; the CV demo A4 panel links to both print routes; and `/docs/cv-demo/`, `/docs/cv-demo/print/`, and `/docs/cv-demo/jekyll/` all render the normalized CV name `Henriques D. Afonso` from the same demo CV data. | Exact old jekyllcv visual/PDF parity remains tracked under PP-004, PP-102, and PP-103. |
| PP-104D | Timeline/profile content should live inside the profile experience; do not keep a separate profile/timeline nav tab just for it. | Verified | `rtk make build` passed in `marcelofelix/main` on 2026-07-02 after refreshing the local `papyrus` file dependency. Rendered HTML assertions passed: desktop and mobile nav omit `/profile/` and `/timeline/`, the homepage links `More about me` to `/profile/`, the profile route renders `.papyrus-timeline` and `.papyrus-github-preview` cards, and the posts page keeps the timeline as a page action link rather than a nav tab. | Full consuming-site profile tab/template behavior remains tracked under PP-104. |
| PP-105 | Support TOML and JSON/Markdown export/copy/download. | Verified | Decision: parser packages stay close to the data source, while papyrus ships demo sources, adapter examples, normalization, and export helpers. Current demo source is `public/demo/users.toml`; older YAML sample was removed after the profile TOML migration. | Add site-owned parser adapters only in consuming repos if they need packaged helpers. |
| PP-105A | CV data export should support download/copy as JSON and Markdown. | Verified | `make verify-browser` passed on 2026-07-01 and checks `/docs/cv-demo/` opens the JSON and Markdown export panels, validates `cv.json`/`cv.md` data-download links with encoded payloads, copies normalized JSON containing `"sections"` and `"name": "Henriques D. Afonso"`, and copies Markdown containing `# Henriques D. Afonso` plus `## Professional Experience`. | Keep browser copy/download checks if `PapyrusCvExportActions` changes. |
| PP-105B | CV normalization should preserve jekyllcv-shaped fields and normalize scheme-less URLs. | Verified | `make verify-cv` passed on 2026-07-01 and checks name, avatar, bio, profile URL, email parts, links, pages, nationality, languages, sections, groups, ranges, and `<br>` description conversion. | Keep this covered by `make verify-cv`. |
| PP-106 | CV/profile should use Pure components where useful, such as timeline and GitHub repo preview. | Verified | `/docs/cv-demo/` now imports and renders `PapyrusTimeline`, `PapyrusGithubCard`, and `PapyrusProjectList` inside the CV/profile demo flow. `make verify-browser` passed on 2026-07-01 and checks the CV demo has at least five template project cards, a GitHub preview with `data-repo="marcelofpfelix/papyrus"` linking to the repo, and at least two shared timeline events including the Portugal CEO event. | Exact Pure visual/API parity remains covered by broader Pure comparison rows; this row verifies useful shared-component integration in the CV/profile demo. |
| PP-107 | Profile tab should not be shown as nav link; profile should live in website/home flow. | Verified | `rtk make build` passed in `marcelofelix/main` on 2026-07-02. Rendered HTML assertions passed: desktop and mobile nav omit `/profile/`, and the homepage flow links `More about me` to `/profile/` while `/profile/` links to `/profile/print/`. | Keep future profile access in homepage/about flows unless a nav tab is explicitly re-requested. |
| PP-108 | CV route should allow changing font, color, and page template. | Verified | `/docs/cv-demo/` now has CV controls for template, color, and font. `rtk make verify-browser` passed on 2026-07-01 checking template switching from terminal to cards and A4, `aria-pressed` state, visible/hidden template panels, theme profile switch to `pure`, and font profile switch to `readable`. | Exact consuming-site profile tab integration remains tracked separately under PP-104/PP-109. |
| PP-109 | CV/profile can have different page templates. | Verified | `make verify-browser` passed on 2026-07-01 and checks `/docs/cv-demo/` exposes four template controls (`terminal`, `cards`, `timeline`, `a4`), toggles `aria-pressed`, updates `data-papyrus-cv-template`, shows/hides matching panels, and renders `.papyrus-cv--cards` plus `.papyrus-cv--a4` from the same normalized CV data. | A future template registry could still improve extensibility, but the requested multi-template user-facing behavior is verified. |
| PP-110 | Re-read old `jekyllcv` and `bandonga/cv` sources before claiming CV parity. | Verified | Re-read local `jekyllcv` sources (`_layouts/cv.html`, `_includes/cv-templates/default.html`, `_includes/cv-profile.html`, `_includes/cv-sections.html`, and `_sass/jekyllcv/_layout.scss`) plus the current local `bandonga.com/cv/marcelo` source in `marcelofelix/main` (`src/data/profile.ts`, `src/pages/profile/index.astro`, `src/pages/profile/print.astro`, and `public/cv/marcelo/`). Added `.agents/cv-source-comparison.md` with the observed page/profile/section/print structure, papyrus mapping, and explicit non-parity boundaries. `make verify-cv` passed on 2026-07-02 and checks that comparison note. | Exact old jekyllcv visual/PDF parity and consuming-site profile integration remain separate under `PP-004`, `PP-102`, `PP-103`, and `PP-104`. |
| PP-180 | Profile export icons and Resume/Timeline/Projects/A4 buttons should be centered; timeline should include date-less Interests and Education entries. | Verified | `src/pages/profile/index.astro` centers `.cv-controls`, `.cv-actions`, `.cv-template-tabs`, and each action/tab's inline contents. `src/utils/cv-profile-data.ts` treats `education` and `interests` as date-optional timeline sections. `CI=true pnpm build`, `CI=true make verify-cv`, and a focused generated `/profile/` assertion passed on 2026-07-10, confirming the built timeline includes `Frontend documentation and accessibility` plus `Interests` entries with date-empty markers. | Keep browser screenshot evidence separate if exact visual centering needs pixel comparison. |
| PP-181 | Public posts, pages, README, docs, and generated artifacts should read as useful Papyrus examples; development-only Markdown should stay repo-only. | Verified | Reviewed the current public route/content set across posts, docs, home/about/projects/search/tags/404/profile routes, README, demo notes/projects, hidden direct post behavior, generated `llms`/AI artifacts, and built route metadata. Strengthened `.agents/scripts/verify-public-content.mjs` so it now rejects filler demo notes/projects, checks the homepage install-guide entry point, confirms public notes describe real features, and ensures repo-only docs such as `guide`, `request-audit`, `status-roadmap`, `pure-parity`, and `theme-spec` are not linked from the public docs index. On 2026-07-10, public docs headings in feature-map, content-structure, docs index, graph, feature config, CV demo, about, and the content-tree fixture were polished from checklist/title-case labels to sentence-case user documentation, and the public-content/browser verifiers were updated to keep those headings intentional. The same pass normalized the install guide and Markdown authoring post headings, replaced the scaffold GitHub/profile placeholders with site-owned placeholders, normalized public content-tree fixture titles, and added the old labels/placeholders to the generated public-content forbidden list. `CI=true pnpm build`, `node .agents/scripts/verify-public-content.mjs`, `node .agents/scripts/verify-content-model.mjs`, `node .agents/scripts/verify-demo-coverage.mjs`, `node .agents/scripts/verify-pure-parity.mjs`, `node .agents/scripts/verify-browser.mjs`, `node .agents/scripts/verify-markdown-demo.mjs`, `node .agents/scripts/verify-ai-artifacts.mjs`, and `node scripts/check-built-links.mjs` passed after the copy passes. Earlier same-day coverage also passed `node .agents/scripts/verify-docs-structure.mjs`, `node .agents/scripts/verify-readme-scope.mjs`, and `node .agents/scripts/verify-features.mjs`. | This proves the current public-content contract and generated route/artifact consistency. It does not claim pixel-perfect Papyrus/Pure visual parity; keep browser/screenshot parity in the dedicated UI verifier rows. |

## AI-first and mobile-first

| ID | Request / behavior | Status | Evidence | Next action |
| --- | --- | --- | --- | --- |
| PP-120 | Document what is missing for AI-first. | Verified | `.agents/status-roadmap.md` and `/docs/ai-mobile/` list missing AI-first items. | Keep updated as items move. |
| PP-121 | Document what is missing for mobile-first. | Verified | `.agents/status-roadmap.md` and `/docs/ai-mobile/` list mobile-first gaps. | Add screenshots before claiming implementation. |
| PP-122 | Configure features to disable. | Verified | `make verify-features` passed on 2026-07-01 and checks default feature flags plus site overrides for built-in layout/post features. | Browser-test disabled UI separately when layout behavior changes. |
| PP-123 | Community plugins similar to Starlight. | Verified | `make verify-features` passed on 2026-07-01 and verifies public `definePapyrusPlugin`/`resolvePapyrusPluginConfig`, plugin feature defaults, site override precedence, capability aggregation, setup lifecycle hooks (`addCapability`, `setFeatureDefaults`), and the external `examples/papyrus-kbd-plugin` package fixture with peer dependencies and default plugin export. | This verifies the lightweight papyrus community-plugin contract; Starlight-specific plugin ideas remain tracked under their own rows. |
| PP-123A | Community plugin support should be important/first-class, not only a roadmap note. | Verified | Added `examples/papyrus-kbd-plugin` as a tiny external plugin package fixture with package exports, peer dependencies on `papyrus`/`astro`, and a default plugin definition imported from the public `papyrus/utils` API. `docs/guide.md` documents it as the minimum community-plugin package shape, `package.json` includes `examples` in the published file list, and `make verify-features` passed on 2026-07-01 by importing the fixture and resolving its capabilities through `resolvePapyrusPluginConfig`. `make verify-package` also passed. | Broader Starlight-like lifecycle compatibility remains partial under PP-123; this row verifies first-class local API/docs/example-package support. |
| PP-123B | Evaluate specific Starlight plugin ideas before implementing local equivalents: site graph, kbd, auto-sidebar, contextual menu, Telescope. | Verified | `docs/guide.md` now has a Starlight plugin idea comparison matrix for `starlight-site-graph`, `starlight-kbd`, `starlight-auto-sidebar`, `starlight-contextual-menu`, and `starlight-telescope`, with usefulness and papyrus decisions for each. `make verify-features` passed on 2026-07-01 and checks those comparison notes plus the warning not to close broader community-plugin rows just because ideas are listed. | Keep implementation work tracked under specific feature rows such as graph/search/backlinks or plugin lifecycle rows. |
| PP-124 | Consider Starlight plugin/component ecosystem before local implementations. | Verified | Added `.agents/starlight-comparison.md` after checking current Context7 `/withastro/starlight` docs on 2026-07-01. The record covers public component imports such as `@astrojs/starlight/components` `Aside`, `Tabs`, `TabItem`, component override/reuse patterns such as `@astrojs/starlight/components/SocialIcons.astro`, sidebar autogeneration, markdown/prose references, plugin ecosystem boundaries, and maps those checks back to papyrus rows. `docs/guide.md` links to the record, `AGENTS.md` points to the verifier, and `make verify-features` passed on 2026-07-01 checking the comparison content. | Keep adding row-specific Starlight source notes when future implementations use Starlight as a reference. |
| PP-124A | Features that already exist in Pure should be treated as priority candidates to adapt before custom roadmap work. | Verified | Added `.agents/pure-parity.md` with Pure-first checklist entries for theme controls, code blocks, timeline/profile, GitHub repository preview, posts index, and markdown styles. `AGENTS.md` now points to `make verify-pure-parity`. `make verify-pure-parity` passed on 2026-07-01 and checks the required feature areas, Pure source references, owner/status/evidence columns, and package script/bin wiring; `make verify-package` also passed after adding the bin. | Keep individual feature rows partial until their Pure visual/API parity is separately verified. |
| PP-125 | Automatic `llms.txt` with all documentation URLs during build. | Verified | `generate-llms.mjs` now scans `src/pages/docs` in addition to `src/content/posts` and emits a `# Documentation URLs` section. `make verify-ai` passed on 2026-07-01 and dynamically walks every `.astro`/`.md`/`.mdx` file under `src/pages/docs`, asserting each derived `/docs/.../` URL appears in `llms.txt`; `make build` also passed and runs this generator automatically before Astro. | Keep `make verify-ai` updated if docs route conventions change. |
| PP-126 | Site graph/telescope-style roadmap ideas. | Verified | `make verify-ai` verifies static `graph.json` nodes/edges, and `make verify-browser` passed on 2026-07-01 checking `/docs/graph/` search, type filtering, node focus, focused connection details, and reset behavior. The guide still records full Telescope-style global search/backlink exploration as future work rather than part of this local graph demo. | Keep future Telescope/global-search work as separate feature rows if prioritized. |
| PP-127 | AI-first blog should expose JSON-LD, source, citations, indexes, graph, and stable IDs. | Verified | `make verify-ai` passed on 2026-07-01 and now verifies llms files, all docs URLs, stable IDs across post/tag/project/note/CV JSON indexes, `search.json`, graph node/edge exports, metadata validation, generated tag RSS feeds, and stale artifact detection. `PapyrusAiMetadata` also emits JSON-LD `@id`, and `PapyrusPostLayout` passes a stable post id derived from canonical URL. | Broader semantic search ranking/backlink interactions can still be separate future rows, but the requested AI-first metadata/index/source/citation/stable-ID surface is verified. |
| PP-128 | Mobile-first requires real device/viewport QA. | Verified | `make verify-responsive` passed on 2026-07-01 and checks 390px mobile plus 768px tablet touch contexts for home, posts, post demo, code demo, and CV demo. It writes responsive screenshots, asserts no horizontal overflow, required route content, and no visible touch targets under 40px. `make verify-browser` still covers the dedicated mobile header compaction behavior. | A physical-device pass or mobile Lighthouse budget can be added later, but the requested repeatable viewport/touch QA is now present. |
| PP-129 | Decide the best commenting system instead of only showing placeholder stats. | Verified | `docs/guide.md` documents `Default comment choice: giscus`, `/docs/features/` now exposes the same decision, and `make verify-browser` passed on 2026-07-01 checking the rendered comments docs mention `giscus`, site-owned optional config, and `PapyrusGiscusComments`. A 2026 follow-up in `.agents/comments-provider-research.md` compared AstroPaper/Giscus, Astro Cactus/Webmentions, Astro Pure/Waline, Hugo/Minimal Mistakes provider matrices, Eleventy-style snippets, and Reddit/community sentiment. The decision remains Giscus as the built-in default for developer blogs, with custom-provider hooks preferred over a large built-in matrix. | Keep live comments rendering tracked under PP-059D. If Giscus is rejected after real use, the top alternative is Cusdis for lightweight non-GitHub comments. Treat Waline as the richer backend-owned fallback and Webmentions as an optional IndieWeb/social-reply layer, not the direct comment-box replacement. |
| PP-130 | Lighthouse score should be checked by a script or GitHub Action if quick, and regressions explained. | Verified | `.agents/scripts/verify-lighthouse.mjs` now prefers system Chrome when present, uses provided throttling for the local static-server gate, defaults `LIGHTHOUSE_MIN_SCORE` to `100`, discovers generated `index.html` routes plus `/404.html`, and has no default route exclusions. `pnpm run verify:lighthouse` passed on 2026-07-08 across every discovered Papyrus route with 100 for performance, accessibility, best-practices, SEO, and agentic-browsing; the route set included `/profile/print/` and no `/profile/print-2/`. An all-route Lighthouse loop also passed in `marcelofelix` on 2026-07-08 for every generated route with 100 thresholds. | Keep exclusions opt-in only through `LIGHTHOUSE_EXCLUDE_PATTERN`; every generated page should keep a 100 default threshold. |
| PP-131 | If Lighthouse is lower than AstroPapyrus/Pure, diagnose concrete causes instead of hand-waving. | Verified | The 2026-07-08 100-score pass fixed the concrete blockers found during the failed runs: Playwright Chromium `NO_FCP` by preferring system Chrome, tag/project touch-target sizing, brand/share/citation label-in-name mismatches, static GitHub cards to avoid 404 console errors, non-loading static GitHub preview contrast, explicit demo image dimensions, dark callout contrast tokens, accessible graph SVG semantics, absolute canonical URLs, and the missing consuming-site `/icons/code.svg` sprite. On 2026-08-15, Papyrus stopped requiring that sprite by inlining the Shiki code-block SVG icons in the transformer and deleting the demo `public/icons/code.svg`. | Treat future non-100 reports as route-specific regressions and inspect the generated report before changing thresholds. |

## Deployment and dependencies

| ID | Request / behavior | Status | Evidence | Next action |
| --- | --- | --- | --- | --- |
| PP-140 | Cloudflare deploy instructions without doing deploy. | Verified | `make verify-docker` passed on 2026-07-01 and checks `docs/deploy.md` documents the repeatable `make deploy-demo` command while separating it from one-time Cloudflare setup. Actual deploy remains deferred under PP-032. | Do not run deploy until re-authorized. |
| PP-141 | Use latest app/library versions where possible. | Verified | Added `make verify-deps` / `pnpm run verify:deps` as a live registry check. It passed on 2026-07-01: `pnpm outdated --format table` reports only `typescript 5.9.3 -> 6.0.3`; `pnpm view typescript@latest` confirms 6.0.3; `pnpm view tsconfck@latest` confirms `tsconfck@3.1.6` still peers `typescript: ^5.0.0`; and `pnpm why tsconfck` shows the blocker comes through `astro-pure@1.4.6` -> `astro@6.1.8`. `.agents/status-roadmap.md` records the hold and `make verify-package` passed after adding the verifier bin. | Re-run `make verify-deps` before release; upgrade TypeScript when the Pure/Astro peer chain accepts TypeScript 6. |
| PP-142 | Add Dockerfile/Make instructions. | Verified | Added `Dockerfile`, `make docker-build`, `make docker-run`, `make docker-stop`, `make verify-docker`, and Docker preview docs. `make verify-docker` and `make verify-package` passed on 2026-07-01. | A real `docker build` smoke test can be run later when Docker runtime state is in scope. |
| PP-143 | Make commands should support local dev on different ports for package and consuming repo. | Verified | `papyrus/main` Makefile uses `PORT ?= 4326`, `HOST ?= 0.0.0.0`, `SITE_HOST ?= 192.168.1.102`, `make dev`/`preview` use `HOST`, and `make serve` binds the static server to `SITE_HOST` for LAN access. `marcelofelix/main` uses `PORT ?= 4325`, `HOST ?= 0.0.0.0`, `SITE_HOST ?= 192.168.1.102`, and its `serve` target also binds to `SITE_HOST`. Both repos call `dev-stop` before binding and kill existing listeners on their port. Papyrus `make serve` was verified on 2026-07-08 by `curl -I http://192.168.1.102:4326/` returning `HTTP/1.0 200 OK`. | Starting long-running dev servers remains manual; the Make targets are restart-safe, port-separated, and LAN-hosted for static preview. |
| PP-144 | Blocked decision: choose whether Marcelo keeps local markdown helpers or switches to papyrus equivalents, then clean duplicates. | Verified | The alert/callout conflict is resolved: both `papyrus` and `marcelofelix` use `rehype-callouts`, and `marcelofelix` no longer depends on `remark-github-blockquote-alert`. On 2026-07-10, `marcelofelix` switched `astro.config.mjs` to import `remark-mermaid-blocks` and `rehype-task-list-labels` from `astro-papyrus`, removed local `src/markdown/remark-mermaid-blocks.mjs` and `src/markdown/rehype-task-list-labels.mjs`, and `rg` confirmed no remaining `src/markdown` helper imports. `CI=true pnpm exec astro check` and `CI=true pnpm build` passed in `marcelofelix` after refreshing the local `file:../../papyrus/main` dependency. | Keep markdown helpers in Papyrus unless a consuming site needs genuinely site-specific behavior. |
| PP-145 | Dockerfile and Make/deploy docs should distinguish one-time setup scripts from repetitive make targets. | Verified | `docs/deploy.md` now separates Docker preview, one-time Cloudflare setup, and repeatable deploy commands; `make verify-docker` passed on 2026-07-01 checking those sections and Make targets. | Keep one-time account/token setup out of Makefile targets. |
| PP-146 | Pure components should be available through `papyrus`, not only through a transitive `astro-pure` dependency. | Verified | Added stable pass-through exports for `papyrus/pure`, `papyrus/pure/user`, `papyrus/pure/advanced`, `papyrus/pure/pages`, `papyrus/pure/basic`, `papyrus/pure/utils`, and `papyrus/pure/libs`. `make verify-package` passed on 2026-07-02 and checks these public exports resolve to package files while keeping `astro-pure` as the dependency boundary. | Add a consuming-site fixture import test if the package API expands beyond pass-through barrels. |
| PP-147 | Prefer wrapping/importing Pure components before maintaining local copies for equivalent features. | Verified | `.agents/pure-parity.md` now has a `Pure wrapper decisions` table for Pure user/advanced/page components, `PapyrusGithubCard`, `PapyrusLinkPreview`, `PapyrusTimeline`, `PapyrusPostList`, `PapyrusToc`, back-to-top, and Papyrus runtime behavior. Public Pure groups are available through `papyrus/pure/*`; local Papyrus variants are justified where Pure depends on private plugin imports, Pure collection/routing assumptions, UnoCSS utility classes, specific header/content IDs, or Papyrus-only behavior. `make verify-pure-parity` checks the decision table and public pass-through exports. | Revisit individual rows if Pure publishes more stable plugin/component subpaths or if a local Papyrus variant becomes unnecessary. |
| PP-148 | Code highlighting should match Pure's Shiki behavior from the upstream Pure docs/source, not the older Papyrus runtime. | Verified | Cloned and inspected `cworld1/astro-theme-pure`. The docs page `src/content/docs/integrations/content-present.mdx` demonstrates `title=`, diff notation, and highlight notation, while upstream `astro.config.ts` wires Shiki transformers for diff, highlight, remove-escape, title, language, copy, and collapse. `papyrus` now copies that transformer setup under `src/shiki/`, exposes it as `papyrus/shiki`, wires it in `astro.config.mjs`, and maps Pure CSS behavior into `papyrus.css`. `rtk make build`, `rtk make verify-markdown`, `rtk make verify-pure-parity`, and `rtk make verify-browser` passed on 2026-07-02. | Exact pixel comparison against upstream Pure can be added later if needed. |
| PP-149 | Reuse maintained markdown/integration plugins where compatible before local markdown helpers. | Verified | Pure's integration markdown plugins remain private and should not be deep-imported. For callouts, AstroPapyrus provides a better public precedent: `rehype-callouts` with Obsidian theme CSS, now adopted in `papyrus` and `marcelofelix`. `verify-pure-parity` checks the `rehype-callouts` dependency/import/CSS wiring and the Pure private-plugin boundary. | If Pure publishes public plugin exports, replace compatible local helpers with stable imports. Until then, keep local helpers only where no maintained public plugin covers the behavior, such as Mermaid fenced rendering, artifact cards, task checkbox labels, and Papyrus source/share actions. |
| PP-150 | Split large post runtime behavior into optional feature modules/plugins instead of one custom script block. | Verified | Post runtime behavior remains split into `PapyrusPostActionsRuntime` for share/source/citation copy, `PapyrusMediaRuntime` for optional artifact hydration, Mermaid rendering, and image/SVG zoom, and `PapyrusBackToTopRuntime` for the scroll popup. The old code-block runtime was removed to match Pure behavior. `PapyrusPostLayout.astro` wires remaining modules through feature flags such as `share`, `sourceActions`, `artifactLinks`, `imageZoom`, `mermaid`, and `backToTop`. | Further code splitting can split `PapyrusMediaRuntime` if bundle size work becomes a priority. |
| PP-151 | Maybe/blocked: evolve papyrus features into a Starlight-like plugin architecture that stays reusable as Astro components and easy to upstream into Pure. | Deferred | Marcelo asked on 2026-07-02 to create a maybe blocked task for this. Current package has reusable Astro exports, split runtime components, feature flags, Pure pass-through exports, and a lightweight `definePapyrusPlugin` API, but not a full Astro/Starlight integration lifecycle that can inject routes, markdown config, scripts, and components automatically. | When prioritized, design a real plugin lifecycle with stable extension points, fixture packages, feature-gated Astro integration hooks, docs/demo coverage, and a Pure-upstream checklist. Until then, future features must follow the guide rule: isolated component/helper first, feature flag where applicable, no private Pure/Starlight internals, and minimal Papyrus-specific assumptions. |
| PP-152 | Add theme profiles for Tokyo Night, Kanagawa, Rosé Pine, Everforest, Dracula, Gruvbox, and Nord so consuming sites inherit them from papyrus. | Verified | Added `tokyo-night`, `kanagawa`, `rose-pine`, `everforest`, `dracula`, `gruvbox`, and `nord` theme CSS files, imported them from `papyrus.css`, exported them from `package.json`, exposed them in provider/footer/CV controls, and documented them. Renamed the public Gruvbox profile/export from `gruvbox-material-hard` to `gruvbox`. `rtk make verify-theme`, `rtk make verify-package`, `rtk make verify-pure-parity`, `rtk make build`, and `rtk make verify-browser` passed in `papyrus` on 2026-07-02. Refreshed the local file dependency in `marcelofelix`, confirmed `node_modules/papyrus/src/styles/themes` has `gruvbox.css` and no old `gruvbox-material-hard.css`, and `rtk make build` passed there. | Keep theme additions covered by `verify-theme` and consuming build checks when adding more profiles. |
| PP-153 | papyrus should use the animated Twinkling SVG logo and visible `papyrus` site title by default, while marcelofelix can keep its `~ $` blinking cursor override. | Verified | Added configurable brand props to `PapyrusBaseLayout`/`PapyrusHeader`: default `brandMark="twinkle"` with visible `brandTitle`, and legacy `headerTitle` selects the terminal cursor mark. `papyrus-logo --twinkle` and `papyrus-mobile-icons --twinkle` generate the default Twinkling SVG/favicon/mobile icons; passing text such as `~ $` keeps the terminal asset path. The Twinkling SVG/header mark now animates with CSS `twinkle` keyframes and disables motion under `prefers-reduced-motion: reduce`. `rtk make verify-assets`, `rtk make build`, and `rtk make verify-browser` passed in `papyrus` on 2026-07-02; browser verification checks normal `twinkle` animation and reduced-motion `none`. After refreshing the local dependency, `rtk make build` passed in `marcelofelix`; generated HTML still shows `~ $` plus `.papyrus-cursor`. | Keep `verify-assets` and consuming build checks covering both default and terminal override paths. |
| PP-154 | papyrus should default to Gruvbox/readable, expose `Posts Docs Projects Profile About`, delete the standalone post demo, and absorb marcelofelix search/profile code for review; marcelofelix should keep Catppuccin/code and only show `Posts Projects About` in nav. | Verified | Deleted `/post-demo/`, changed demo nav to `Posts Docs Projects Profile About`, copied the working dynamic search page and profile routes for review, and updated verifiers/docs to use `/docs/code-demo/`. Papyrus profile assets were later switched back to the jekyllcv demo assets and `public/cv/marcelo/*` was removed from Papyrus. `rtk make build`, `rtk make verify-markdown`, `rtk make verify-pure-parity`, and `rtk make verify-browser` passed in `papyrus` on 2026-07-02. Refreshed `marcelofelix`, kept docs/profile routes hidden from `site.nav`, and `rtk make build` passed there; generated HTML shows only `Posts`, `Projects`, and `About` in nav. | There is still a separate pending decision for duplicate markdown helper wiring in `marcelofelix` versus package-owned helpers; see the blocked/deferrable markdown rows. |
| PP-155 | Rename the npm package identity to `astro-papyrus`, while keeping the current worktree path and Papyrus component vocabulary for now. | Verified | `package.json` now uses `name: "astro-papyrus"`, package self-imports in demo pages use `astro-papyrus/*`, the local package verifier expects the new name, and `marcelofelix` imports/dependency use `astro-papyrus`. `pnpm install` refreshed package-manager state in both repos. `rtk make verify-package`, `rtk make verify-readme`, `rtk make verify-features`, `rtk make verify-pure-parity`, and `rtk make build` passed in `papyrus` on 2026-07-02; `rtk make build` passed in `marcelofelix`. | The only remaining old literal is in `marcelofelix` local dependency metadata because it points at the current physical worktree path; changing that requires an approved worktree directory move. |
| PP-156 | Release task: publish under `astro-papyrus` with a real package, not an empty npm name reservation. | Verified | Package metadata now targets the real public package at version `0.2.2`: `name: astro-papyrus`, `private: false`, MIT license, repository/homepage/bugs URLs, keywords, public `publishConfig`, explicit `files`, README, `LICENSE`, and `CHANGELOG.md`. Public install examples now target `^0.2.2`, and npm bin paths preserve the `papyrus-*` CLIs. `CI=true pnpm run verify:release`, `CI=true pnpm run verify:package`, `CI=true pnpm run verify:readme`, `CI=true pnpm run verify:config`, `CI=true pnpm run verify:features`, and `CI=true pnpm build` passed on 2026-08-13. Marcelo completed npm security-key authentication, `astro-papyrus@0.2.2` was published to npm, and `pnpm view astro-papyrus version --json` returned `"0.2.2"`. | Consumers still use the GitHub dependency until the npm release passes the repo's `minimumReleaseAge: 10080` gate. `pnpm add astro-papyrus@^0.2.2` currently fails because the package was published within the 7-day maturity window. Do not bypass this policy unless explicitly requested. |
| PP-157 | v0.2.0 release task: reach AstroPapyrus feature-parity decisions before claiming parity. | Verified | Added repo-only `.agents/astropapyrus-parity.md` with a per-feature decision table for typed config, Pagefind search, dynamic OG image routes, edit/source links, share links, pagination/per-index config, scheduled posts, archive toggle, pages collection, centralized language/direction/timezone/profile/verification config, MDX, and sitemap integration. Every row now has an explicit v0.2.0 decision: implemented, implemented as demo pattern, deferred with rationale, or site-owned. `node .agents/scripts/verify-docs-structure.mjs` passed on 2026-07-10 and checks that the record exists, stays out of public docs routes, and contains no ambiguous `Partial` or open-ended `Implement next` rows. | Do not claim full AstroPapyrus parity in release notes; only claim the implemented rows and keep deferred rows scoped to future work. |
| PP-158 | v0.2.0 release task: move user-facing serializable config into `papyrus.config.toml` loaded by package helpers. | Verified | Added `papyrus.config.toml`, typed `astro-papyrus/config` exports (`definePapyrusConfig`, `resolvePapyrusConfig`, `parsePapyrusConfigToml`, `loadPapyrusConfig`), package export wiring, script-side TOML-first `siteConfig()` fallback, and demo data wiring for nav, social links, and post-card defaults. The install guide documents `papyrus.config.toml` and `loadPapyrusConfig`. `make verify-config`, `CI=true pnpm exec astro check`, `CI=true pnpm build`, `node .agents/scripts/verify-public-content.mjs`, `CI=true pnpm run verify:package`, and `make verify-release` passed in Papyrus on 2026-07-10. `marcelofelix` now has its own `papyrus.config.toml`, keeps `src/site.config.ts` only as a compatibility bridge around `loadPapyrusConfig`, and `CI=true pnpm exec astro check` plus `CI=true pnpm build` passed in the showcase repo after the migration. | Future config expansion should keep serializable site defaults in TOML and leave route composition, Astro integrations, private credentials, and content ownership in the consuming site. |
| PP-159 | v0.2.0 release task: force `papyrus` branding and command names after the `astro-papyrus` package rename. | Verified | Command names are now `papyrus-*`, demo/generated asset filenames use `papyrus`, visible title/logo/footer/project/docs text use `papyrus`, the example plugin fixture was renamed to `examples/papyrus-kbd-plugin`, and a full tracked-source scan in the theme repo finds no old package-name literals. `rtk make verify-package`, `rtk make verify-readme`, `rtk make verify-features`, `rtk make verify-pure-parity`, and `rtk make build` passed in the package repo; `rtk make build` passed in `marcelofelix`. | Keep `Papyrus*` component names and `papyrus-*` CSS classes as generic design/API vocabulary unless a separate API rename is requested. |
| PP-160 | Home pages in Papyrus and marcelofelix should show `more about me` before Posts, then pinned posts plus 3 latest posts, then pinned projects plus 3 projects, with right-aligned `more posts`/`more projects` links at the end of each section and no borders on those links. | Verified | Papyrus demo home and `marcelofelix` home now use the same flow: hero with right-aligned borderless `more about me` linking to `/about/`, Posts with pinned-first plus three non-pinned posts followed by borderless `more posts`, and Projects with pinned-first plus three non-pinned projects followed by borderless `more projects`. Added `.papyrus-section-more` and `.papyrus-home-more` styles with zero link borders. `marcelofelix` defaults `homePostLimit` and `homeProjectLimit` to `3`, and its Papyrus project is explicitly pinned. `rtk make build` passed in `papyrus`; `rtk make build` passed in `marcelofelix`; `rtk make verify-browser` passed and checks home action links, right alignment, end-of-section placement, and zero borders. | Keep this contract in browser checks if the home page or section action styling changes. |
| PP-161 | Post lists should show new labels for posts published in the last 30 days and accent-colored metadata for fresh updates. | Verified | `PapyrusPostList` now adds a `new` `.papyrus-meta-item` with the sparkles icon when `isNewPost()` matches the 30-day window. Fresh updates no longer render a separate refresh/update badge; instead, the date metadata gets `.papyrus-post-recently-updated` accent color when `hasFreshUpdate()` matches. The freshness helpers live in `src/utils/posts.ts` and remain exported through `astro-papyrus/utils`. `.agents/scripts/verify-content-model.mjs` covers the 30-day utility behavior. | Freshness is evaluated at build/render time for static output. If a site needs client-live freshness after a long-lived deploy, add a small runtime hydrator instead of changing content dates. |
| PP-162 | Docs should follow Starlight-style content recommendations: section metadata from `index.md`, docs ordering by explicit index/order instead of publish date, and posts can also appear as docs because docs and posts share content sources. | Verified | Added a `docs` content collection with `src/content/docs/index.md` section metadata, added `src/utils/docs.ts` helpers to build ordered docs groups, and updated `/docs/` to combine explicit docs pages with posts whose frontmatter has `docs: true` or `docs: { section, order, title, description }`. Post-backed docs entries keep source metadata internally, but render as normal docs index links. `.agents/scripts/verify-docs-structure.mjs` checks the collection/index/frontmatter wiring, and `.agents/scripts/verify-browser.mjs` checks `/docs/` renders Getting started, Authoring, and Feature references sections plus post-backed docs links from the shared source model. | This is Starlight-inspired structure, not a Starlight dependency. If docs grow further, add pagination/sidebar helpers around the same `index.md` metadata instead of reintroducing date ordering. |
| PP-163 | Project cards should be closer to Pure's GitHub-card shape and expose explicit docs/website/repo links. | Verified | Compared local `PapyrusProjectList` with Pure `astro-pure/components/advanced/GithubCard.astro`. `PapyrusProjectList` now supports explicit `links`, optional visible `text` for labels such as `marcelofpfelix/papyrus`, label-specific icons for package/docs/website/demo/data/graph/repo actions, duplicate-href filtering that prefers a single `repo` action for repository URLs, borderless action links, and no lower footer metadata row for `pinned`, `demo`, `Astro`, or stars. It also avoids both the duplicate top GitHub link and the duplicate standalone `/projects/` GitHub preview. `public/demo/site-data.json` includes package/docs/website/repo links for papyrus and shows the copied Marcelo photo on the CV profile project card. `marcelofelix/main` points `astro-papyrus` at `../../papyrus/main` and uses the same action-link data shape in `src/data/projects.ts`. `.agents/pure-parity.md` records the Papyrus-specific local implementation choice. `public/ai/projects.json` was regenerated with the new link arrays. `pnpm exec astro check`, `make verify-pure-parity`, `pnpm build` in `marcelofelix`, and escalated `make verify-browser` passed on 2026-07-06; browser checks assert no duplicate project repo header, no standalone projects GitHub preview, borderless project action links, no project footer metadata rows, the papyrus package/docs/website/repo links, and the CV photo/demo/data links on `/projects/`. | `make verify-ai` is still blocked by pre-existing demo post metadata issues: several posts are missing `slug` and/or `license`. Fix those before using full AI verification as release evidence. |
| PP-164 | Project card image and description should be primary links like post cards. | Verified | `PapyrusProjectList` now wraps project images in `.papyrus-project-image-link` when a project has `href`, and wraps descriptions in `.papyrus-project-description-link` with the same `href`. `make verify-browser` passed on 2026-07-06 and checks `/projects/` description links plus the CV photo link. A focused Playwright DOM check also verified Papyrus descriptions link to `/docs/`, `/docs/cv-demo/`, and `/docs/ai-mobile/`, while `marcelofelix` project images and descriptions link to `/posts/hello-papyrus/` and `https://github.com/marcelofpfelix/tmx`. | Keep title, image, and description pointing at the primary `href`; keep explicit action links for alternate destinations such as docs/data/repo. |
| PP-165 | Homepage Docs section should use project-card styling with one docs link and no GitHub/extra link cards. | Deferred | This was verified earlier on 2026-07-07, then superseded by PP-166 when the requested visual direction changed from project-like to post-like. | Keep the no-extra-link/no-GitHub-card part through PP-166, but do not keep project-card styling for home Docs. |
| PP-166 | Homepage Docs section should look like the posts list but omit date and tags rows. | Verified | Home Docs uses `papyrus-post-list papyrus-post-list-list papyrus-doc-list` markup with one `/docs/` title/description link and no date/meta rows, tags, project cards, link previews, or GitHub previews. `CI=true make verify-browser` passed on 2026-07-10 and now covers the rendered Home Docs contract in Playwright after building the site and Pagefind index. | Keep this section post-list-like unless the visual direction changes again. |
| PP-167 | Posts page should keep only the list option, while the possible post-list display modes move into a post. | Verified | `/posts/` now renders only the `data-posts-view="list"` section with `PapyrusPostList posts={posts} view="list"`. The compact/cards sections were removed from `src/pages/posts/index.astro`, and `src/content/posts/papyrus-package-shape.md` now documents `list`, `compact`, and `cards` `PapyrusPostList` examples inside the post. `rtk pnpm exec astro check`, `rtk make verify-pure-parity`, `rtk make build`, and `rtk make verify-demo` passed on 2026-07-07. Current public-content checks assert `dist/posts/index.html` contains the list view while the package-shape post documents alternate views with `post-list-view-reference.astro`. | Keep `verify-pure-parity` enforcing list-only `/posts/` plus post-level documentation for alternate views. |
| PP-168 | Post pages should have a back button, clarify the quote/citation action, and make the share button use normal button colors. | Verified | `PapyrusPostLayout` now renders a `Back to posts` action before the post copy/share controls. The old quote/citation control keeps copying `Title. URL`, but its visible label and aria label are now `Copy title + link` / `Copy title and link`. The Share button keeps native-share/copy-link behavior while using the same background and text color as the adjacent normal action button instead of inverted colors. `rtk pnpm exec astro check` and `rtk make build` passed on 2026-07-08. A focused generated HTML/CSS assertion passed for `/posts/markdown-feature-sample/`, and a focused Playwright check passed against `dist/`, confirming `Back to posts`, one back icon, `Copy title + link`, and equal computed Share/title-link button colors. | Full `make verify-browser` still stops on an unrelated older home-list count expectation before reaching post-action checks; keep the focused browser assertion or fix that suite expectation before relying on the full command for this row. |
| PP-169 | Post back action should sit at the top like Pure, post action borders should be removed, and previous/next links should sit left/right. | Verified | Compared Pure's `ArticleBottom.astro` and `Button.astro`: adjacent previous/next links are borderless and sit left/right, while the back-style button is a normal top action. `PapyrusPostLayout` now renders `Back to posts` in `.papyrus-post-back` above `.papyrus-post-header` instead of inside `.papyrus-post-tools`. Post action buttons and the share icon are borderless. `.papyrus-adjacent-posts` no longer has a top separator border, and its Previous/Next links stay left/right. The consuming `marcelofelix` post route now passes `backHref="/posts/"` and `backLabel="Back to posts"` explicitly. `rtk pnpm exec astro check` and `rtk make build` passed in `papyrus`; `rtk pnpm build` passed in `marcelofelix`. Generated HTML/CSS assertions passed for both repos. Focused Playwright checks passed for `papyrus` `/posts/dark-mode-and-search/` with top back link, zero action borders, no adjacent nav border, Previous left and Next right; the rebuilt `marcelofelix` `/posts/hello-papyrus/` check also passed for the top back link and borderless share action. | `marcelofelix` currently has one post, so it cannot prove previous/next alignment locally; package demo browser evidence covers the shared adjacent-post component used by consuming sites. |
| PP-170 | Post back link should be configurable and disabled in `marcelofelix`; the default label should be `Back`; post date, tags, and share/source actions should move to the bottom; icon links should have no background fills. | Verified | `PapyrusPostLayout` now accepts `backHref={false}` to suppress the post back link, defaults the label to `Back`, keeps the optional back link above the post header, and renders the date/updated/reading-time line, tags, and source/share controls in `.papyrus-post-footer` after the prose/stats slots instead of in `.papyrus-post-header`. Post action links/buttons and `.papyrus-share-icon` use transparent backgrounds. The `marcelofelix` post route passes `backHref={false}`. `rtk pnpm exec astro check`, `rtk make build`, `rtk pnpm build` in `marcelofelix`, `rtk make verify-pure-parity`, generated HTML/CSS assertions, and focused Playwright checks for Papyrus `/posts/markdown-feature-sample/` and `marcelofelix` `/posts/hello-papyrus/` passed on 2026-07-08. | Keep generated and browser checks covering header/footer placement, configurable back-link suppression, and transparent computed backgrounds for post action/icon controls. |
| PP-171 | Share button should use the Web Share API when available and fall back to the existing copy-link behavior otherwise. | Verified | `PapyrusPostActionsRuntime` now builds a `ShareData` payload from `document.title`, the current URL, and the page meta description when present, calls `navigator.share()` when available and accepted by `navigator.canShare()`, and keeps the existing copy-link/prompt fallback for unsupported browsers or failed share attempts. It does not use generic sample text. `rtk pnpm exec astro check`, `rtk make build`, a source/runtime assertion, and a focused Playwright check passed on 2026-07-08; the browser check explicitly disabled `navigator.share` to verify copy-link fallback and then stubbed `navigator.share` to verify the native Web Share payload. | Keep both native Web Share and fallback coverage when changing post share behavior. |
| PP-172 | Previous and Next post links should stay left/right on mobile, like Pure. | Verified | The mobile CSS no longer includes `.papyrus-adjacent-posts` in the one-column grid override and no longer resets the last adjacent-post cell to left alignment. Adjacent links now explicitly justify Previous to the start and Next to the end. `rtk pnpm exec astro check`, `rtk make build`, `node --check .agents/scripts/verify-browser.mjs`, and a focused Playwright check at 390px wide passed on 2026-07-08; the browser check verified `/posts/dark-mode-and-search/` keeps two adjacent links, does not collapse the adjacent grid to `1fr`, and places Previous left and Next right. | Keep the mobile assertion in `verify-browser` so future responsive CSS changes do not reintroduce the one-column left-aligned layout. |
| PP-173 | Posts page should show an Archive icon only when hidden posts exist, and hidden posts should be defined by `hidden: true` frontmatter. | Verified | Added `hidden: true` post frontmatter support to the package and consuming-site content schemas, plus shared `routablePosts()` and `hiddenPosts()` helpers. Normal `publishedPosts()` now excludes drafts and hidden posts, while `routablePosts()` keeps hidden non-draft posts buildable. The package demo marks the nested folder-tag post hidden, renders an Archive icon link to `/search/?archive=hidden` because a hidden post exists, excludes that post from normal posts/search/timeline/LLMS/AI/tag-RSS outputs, and still builds `/posts/folder-tags-demo/`. `marcelofelix` supports the same frontmatter but does not render Archive while it has no hidden posts. `rtk pnpm exec astro check`, `rtk make verify-content`, `rtk make verify-pure-parity`, `rtk make build`, generated HTML assertions, focused Playwright Archive assertions, focused generated AI/search/tag-RSS assertions, `rtk pnpm build` in `marcelofelix`, and generated `marcelofelix` Archive-absence assertions passed on 2026-07-08. | Keep Archive visibility conditional on hidden posts and keep hidden posts out of public feeds/indexes unless a dedicated archive surface intentionally exposes them. |
| PP-174 | Post pages should include a `more posts` link at the end of the post. | Verified | `rtk make build` passed on 2026-07-08; generated HTML for `/posts/dark-mode-and-search/` renders `more posts` after `.papyrus-adjacent-posts`; Playwright mobile check confirmed the borderless transparent link points to `/posts/`; `rtk pnpm build` passed in `marcelofelix` and generated `/posts/hello-papyrus/` includes the inherited end-of-post `more posts` link. | Keep `morePostsHref`/`morePostsLabel` configurable for sites that need a different end link. |
| PP-175 | Gruvbox dark theme should use the adjusted foreground, muted, and accent colors. | Verified | Updated the Gruvbox dark tokens from `#d4be98` to `#f5edca`, `#928374` to `#c4bda1`, and `#d8a657` to `#dfb778`; `--papyrus-code-fg` follows the adjusted dark foreground. `rtk make verify-theme`, `rtk make build`, and `rtk pnpm build` in `marcelofelix` passed on 2026-07-08. Focused generated CSS checks confirmed both `papyrus/dist` and `marcelofelix/dist` contain the adjusted Gruvbox dark rule. | Keep light Gruvbox fallbacks unchanged unless requested separately. |
| PP-176 | Sites should have a 404 page that tries to guess similar pages automatically, with search fallback, including `marcelofelix`. | Verified | Added shared `PapyrusNotFound` with static no-JS suggestions, client-side scoring from the missing URL path or `?q=`, and a `q` search form. Added Papyrus `/404.html` seeded from demo posts and docs/static routes, and `marcelofelix` `/404.html` seeded from its real posts plus site routes. Both search pages now initialize from `?q=` before `?tag=`. `papyrus-serve-static` is a Ruby/WEBrick static server with a custom 404 fallback, keeping LAN-IP curl behavior while serving generated themed `404.html` with HTTP 404 for unknown URLs. `rtk pnpm exec astro check`, `rtk make build`, and generated `dist/404.html` assertions passed in Papyrus; `rtk pnpm install` refreshed `marcelofelix`; `curl -I http://192.168.1.102:4326/` returned `HTTP/1.1 200 OK`, and `curl -I http://192.168.1.102:4326/not-found-test/` returned `HTTP/1.1 404 Not Found` with the generated HTML page. | Add browser coverage later if the client-side ranking contract needs to be locked down beyond generated HTML assertions. |
| PP-177 | All CV/profile pages, including `/profile/print/`, should reach 100 Lighthouse scores; the temporary `/profile/print-2/` test route should be removed after acceptance. | Verified | Promoted the accepted `print-2` implementation into the canonical `/profile/print/` route in both Papyrus and `marcelofelix`, then deleted the temporary `/profile/print-2/` route. The canonical print route keeps the A4 visual structure but adds a real `<main>`, a non-`javascript:` print button, favicon/meta description, bold link text with underline on hover/focus, stronger screen contrast, and larger screen hit targets with print-media overrides to keep the printed layout compact. Regular profile/CV routes avoid GitHub API console 404s by rendering static `PapyrusGithubCard` metadata and use bold text plus hover/focus underline for profile text links. The Papyrus `/docs/cv-demo/print/` standalone page has meta/favicon and route-local contrast overrides. `marcelofelix` `llms.txt` emits Markdown links, and `/projects/` has a semantic hidden `h2` before `h3` project cards. `verify-lighthouse` now defaults to min score 100 with no route exclusions, and latest Papyrus plus `marcelofelix` all-route Lighthouse checks passed at 100 for every tested route. | Keep Lighthouse exclusions opt-in only through `LIGHTHOUSE_EXCLUDE_PATTERN`; do not restore default CV/profile/resume exclusions. |
| PP-178 | Home and posts page polish: `about me` label, transparent post-list backgrounds, matching posts-page icons, RSS icon next to `Posts`, no visible `List` label, tag/search action, timeline `view all`, and demo tag RSS examples. | Verified | Papyrus and `marcelofelix` home pages now use `about me`. Both posts pages render `Posts` with an adjacent RSS icon link to `/rss.xml`, use matching Timeline/Tags/Archive/RSS SVG paths through the same `papyrus-actions` pattern, hide list item tags by default on `/posts/`, remove the visible `List` heading, and add a trailing `view all` link to the timeline route. List-view pinned posts now have transparent backgrounds. Papyrus documents `/rss/tags/custom.xml` and `/rss/tags/changelog.xml` inside `src/content/posts/papyrus-package-shape.md`; `markdown-feature-sample` carries the `custom` tag, and `papyrus-package-shape` carries the `changelog` tag so `make rss-tags` generates both feeds. `rtk make rss-tags`, `rtk make build`, `rtk pnpm build` in `marcelofelix`, and generated HTML/RSS assertions passed on 2026-07-08. | Keep RSS examples demo-scoped unless consuming sites add tag-feed generation. Run Lighthouse only before commit, not during every UI iteration. |
| PP-179 | Compare current `marcelofelix` code against Papyrus for reusable pieces, compare Papyrus against Pure for missed reuse, produce a DRY/YAGNI/SOLID/KISS report, run Lighthouse, and fix issues found. | Verified | On 2026-07-10, duplicated CV action bars/share-copy scripts from Papyrus and `marcelofelix` were moved into shared `PapyrusCvActions`, duplicated CV item-title logic moved into `shouldShowCvItemTitle`, and `marcelofelix` profile routes now consume both from `astro-papyrus`. Hidden post indexing was decoupled from robots with explicit `robots` frontmatter support in both repos. Public docs/fixture drift from removed CV/graph demo routes was cleaned up, generated fixture links now point to live profile/CV and `/ai/graph.json` surfaces, and the install/CV/AI docs describe current package behavior. Pure comparison remains tracked by `verify-pure-parity`, which passed after refreshing the profile-route checks. Public-content checks passed with `node .agents/scripts/verify-public-content.mjs`, Papyrus built with `CI=true pnpm build`, and `marcelofelix` built with `CI=true pnpm build`. Lighthouse follow-up fixed the hidden direct post SEO regression, the unsized dark-search post image, and the search accessibility regression: Papyrus focused runs passed 100/100/100/100 for `/posts/dark-mode-and-search/`, `/posts/folder-tags-demo/`, and `/search/`; `marcelofelix` `CI=true pnpm run test:lighthouse` passed its configured home, posts, search, and post routes at 100 thresholds. | Residual non-scoring Lighthouse sub-audits remain: unused shared CSS/JS, label-content-name-mismatch warnings, document latency, render-blocking CSS, and bfcache diagnostics. Keep those as optimization candidates rather than broadening scope before release. Ponytail skills were used as report framing only; no external automation was installed. |
| PP-180 | Public docs should explain the difference between `src` and `public`, where to edit/add pages and posts, and whether Papyrus has too much hardcoded text compared with Papyrus-style starter repos. | Verified | `src/content/posts/install-configure-papyrus.md` now has public `Know src vs public` and `Edit the right file` sections. The post explains `src/` as build-processed source, `public/` as copied static files plus generated artifacts, maps normal edits to `papyrus.config.toml`, `src/content/posts/*.md`, `src/pages/*.astro`, `src/data/profile.toml`, and `public/` assets, and states that long-lived site copy belongs in Markdown/TOML/JSON/data while components should keep only generic fallback labels. Comparison notes: `papyrus-template` has a small 16-file editable surface, while this package repo has many files because it owns reusable components, injected routes, scripts, and demo fixtures; `.agents/pure-parity.md` and `.agents/astropapyrus-parity.md` continue to record that Papyrus should stay a thin Pure wrapper and borrow Papyrus-style publishing ideas without copying AstroPapyrus wholesale. `node .agents/scripts/verify-public-content.mjs`, `CI=true pnpm build`, and generated HTML checks for `/posts/install-configure-papyrus/` passed on 2026-07-10. | Follow-up should move route-local demo prose into Markdown/data if it becomes reusable user documentation rather than package-demo copy. |
| PP-182 | Move agent/tracking-only Markdown and verification scripts out of public docs/scripts, keep the package cleaner like Pure, and update `AGENTS.md` instructions. | Verified | `.agents/` now owns the request audit, status roadmap, Pure/AstroPapyrus/Starlight/CV comparison notes, markdown coverage fixture, and all `verify-*` plus audit-status scripts. `docs/` now keeps user/package docs (`deploy`, `guide`, `release`, `theme-spec`), while `scripts/` keeps package generators and user-facing CLIs. `AGENTS.md` now points agents at `.agents/request-audit.md`, `.agents/status-roadmap.md`, and the existing `make verify-*` commands. `package.json` keeps `pnpm run verify:*` and `make verify-*` entry points but removes all public `papyrus-verify-*` and `papyrus-audit-status` bins. `README.md` documents `scripts` as package/user CLIs and `.agents/scripts` as repo-only verification helpers. Validation passed: `node --check` for key moved scripts, `CI=true pnpm run audit:status`, `verify:readme`, `verify:docs`, `verify:package`, `verify:config`, `verify:pure-parity`, `verify:markdown`, and a temp-cache `npm pack --dry-run --json` parser confirming `.agents/` is excluded from the tarball. | Full `verify:release` was intentionally stopped after several silent minutes in its smoke-fixture install phase; the narrow npm pack boundary was checked separately. Historical audit rows still mention older public verifier-bin names as history, but current package verifiers now reject reintroducing those bins. |
| PP-183 | Review reference repo `.gitignore`, Makefile, pre-commit, and GitHub Actions patterns; keep Papyrus small and aligned with GitHub gitignore guidance. | Verified | Compared local `papyrus-template`, `marcelofelix`, `papyrus/v2`, and archived v2 ignore/Makefile/package-hook surfaces. Checked GitHub's `github/gitignore` repo guidance and Node/macOS/VS Code templates, then expanded `.gitignore` only for relevant Astro/Node/package outputs: dependencies, `.astro`, `dist`, local Lighthouse/screenshots, logs, caches, coverage, env files, npm packs, and OS/editor noise. Replaced the large repeated Makefile with a smaller 85-line Makefile that keeps common dev/build/serve/generation targets and delegates all verifier targets through generic `verify-%`, while preserving build prerequisites for browser/Lighthouse/responsive/demo/A4 checks. Added one minimal `.github/workflows/ci.yml` for install, check, build, and fast structural verifiers; Lighthouse and screenshot/browser checks stay out of routine CI. Kept pre-commit as the existing npm `precommit` CV export script instead of installing hook framework/dependency. Validation passed: Makefile dry-run for representative targets, source assertions for CI and `.gitignore`, `CI=true pnpm run verify:readme`, `verify:docs`, `verify:package`, and `verify:config`. | Git metadata is still broken in this worktree (`fatal: not a git repository: (null)`), so branch/status validation could not be performed locally. |
| PP-184 | On the profile page desktop sidebar, social/contact links should align left instead of centered, and social/contact/info icons should use the same primary color. | Verified | Scoped `/profile/` sidebar CSS now aligns `.papyrus-cv-links` and profile link items to the left with `justify-content: flex-start` and `text-align: left`; Contact link SVGs and Info row SVGs both use `color: var(--papyrus-accent)` and `stroke: currentColor`. `CI=true pnpm exec astro check` passed on 2026-07-10 and source assertions confirmed the scoped CSS rules. | Add browser-computed style coverage if profile sidebar alignment regresses visually in a future UI pass. |
| PP-185 | Profile timeline should only display items with dates. | Verified | `src/utils/cv-profile-data.ts` now builds timeline events only from CV items that have `dates` or a normalized `range`; the old date-optional `education`/`interests` exception was removed, and browser verifier assertions now reject date-less `Interests` entries. `CI=true pnpm exec astro check`, `CI=true pnpm build`, and generated `/profile/` HTML assertions passed on 2026-07-10. | This intentionally supersedes the date-less Interests/Education part of the older PP-180 profile-controls row. |
| PP-186 | On mobile profile, Info should use columns like Contact and match the Contact/social visual style. | Verified | The `/profile/` Info `dl` now uses the same flex wrapping pattern as Contact, and `.cv-profile-detail` uses the same chip-style background, radius, foreground color, gap, and padding as `.papyrus-cv-link-item`; mobile gives info chips a two-column flex basis with shrinkable/wrapping values. `CI=true pnpm exec astro check` and a focused source assertion passed on 2026-07-10. | Add screenshot coverage if exact mobile column spacing becomes part of the visual contract. |
| PP-187 | In `/profile/print/` and `/profile/ast/`, compact duration labels to `2y 1m`, restore the updated-CV mini card, and allow a print-only social/contact subset defaulting to email, LinkedIn, GitHub, and website. | Verified | Added `compactDurationLabel`, used it only in the print and AST routes so `/profile/` keeps long duration labels. Restored a mini card linking to `/profile/` in `/profile/print/` and `/profile/ast/`, but not `/profile/`. Added normalized `print_links` support plus `printProfileLinks`; when omitted it defaults to `email`, `linkedin`, `github`, and `website`, while `src/data/profile.toml` and `public/demo/users.toml` show the explicit setting. Printable routes now use `printProfileLinks` and service labels, so Telegram/Keybase stay on normal `/profile/` but are excluded from print/AST. `CI=true pnpm exec astro check`, `CI=true pnpm run verify:cv`, `CI=true pnpm build`, and focused generated HTML assertions passed on 2026-07-10. | If a consuming site wants a different print set, set `print_links = ["email", "github", "website"]` or similar in its CV TOML. |
| PP-188 | In `/profile/ast/`, do not show website links for experience or education entries. | Verified | Removed the AST route's group URL paragraph from experience/education section headers, leaving organization names and the underlying data intact. `CI=true pnpm exec astro check`, `CI=true pnpm build`, and a generated `/profile/ast/` HTML assertion passed on 2026-07-10, confirming `https://astro.build/` and `https://developer.mozilla.org/` are not shown while the organization names and compact durations remain. | This is AST-only; `/profile/print/` still keeps its existing linked organization names unless requested separately. |
| PP-189 | In `/profile/ast/`, role metadata should show only values, not `Location`, `Dates`, or `Duration` labels. | Verified | The AST route now joins `item.location`, `item.dates`, and compact duration values directly, producing output such as `Remote | 2024 - Present | 2y 6m`. `CI=true pnpm exec astro check`, `CI=true pnpm build`, and a generated `/profile/ast/` assertion passed on 2026-07-10 confirming the label words are absent while values remain. | This is AST-only; `/profile/print/` keeps its existing metadata wording unless requested separately. |
| PP-190 | In `/profile/ast/`, the updated-CV note should not have a border. | Verified | Removed the `.resume-updated` border from the AST route while keeping the note text, spacing, and link intact. `CI=true pnpm exec astro check` and a focused source assertion passed on 2026-07-10. | This is AST-only; `/profile/print/` keeps its current updated-CV note styling unless requested separately. |
| PP-191 | Regression: top action icons outside the A4 CV should not show when printing `/profile/print/` or `/profile/ast/`. | Verified | Added a shared `@media print` rule in `src/styles/cv-print.css` hiding `.no-print` and `.cv-download` with `display: none !important`, so the child `PapyrusCvActions` bar is hidden consistently in both printable routes. `CI=true pnpm exec astro check`, `CI=true pnpm build`, and generated `/profile/print/` plus `/profile/ast/` HTML assertions passed on 2026-07-10 confirming the print-hide rule is present while the screen action bar remains in the HTML. | Keep print-only UI hiding in `cv-print.css`; route-scoped CSS does not reliably target child component roots in Astro. |
| PP-192 | Ensure the latest profile print/AST fixes are also applied in the consuming `marcelofelix` repo. | Verified | Updated `/Users/marcelof/gwt/marcelofpfelix/marcelofelix/main` to export/use `printProfileLinks`, set `print_links = ["email", "linkedin", "github", "website"]`, compact print/AST duration labels through Papyrus utilities, restore the updated-CV note, hide print action icons with the shared `cv-print.css` rule, remove AST group website URL lines, remove AST role metadata labels, and keep full socials on normal `/profile/`. `CI=true pnpm exec astro check`, `CI=true pnpm build`, and generated profile print/AST assertions passed in `marcelofelix` on 2026-07-10. | `marcelofelix` has many pre-existing dirty files; these edits were scoped to the profile data/routes and print CSS. |
| PP-193 | On the Papyrus homepage, the hero should keep only `about me`; move `start here` and `feature map` into the Docs section. | Verified | `src/pages/index.astro` now renders only `about me` in the hero action row. The Home Docs section now lists `papyrus docs`, `start here`, and `feature map` using the existing post-list-like docs styling. `CI=true pnpm exec astro check`, `CI=true pnpm build`, and a generated homepage assertion passed on 2026-07-10, confirming the hero excludes `start here`/`feature map` while the Docs section links to `/docs/`, `/posts/install-configure-papyrus/`, and `/docs/feature-map/`. | This change is Papyrus demo-home scoped; apply separately to consuming sites only if they have equivalent demo docs links. |
| PP-194 | In `/profile/print/` and `/profile/ast/`, the updated-CV note should be centered and should link to the configured website/current CV URL instead of `/profile/`. | Verified | Papyrus and `marcelofelix` now render the updated-CV note from `profile.canonicalCv`, which comes from `canonical_cv` when configured and otherwise falls back through the normalized profile URL. The print-route note has explicit centered text, and AST keeps its centered `fit-content` note. `CI=true pnpm exec astro check`, `CI=true pnpm build`, and generated print/AST assertions passed in both Papyrus and `marcelofelix` on 2026-07-10; Papyrus generated `https://github.com/marcelofpfelix/papyrus`, while `marcelofelix` generated `https://bandonga.com/cv/marcelo`. | If a consuming site wants a different note target, set `canonical_cv` in its CV TOML. |
| PP-195 | Install and use `act` to test the GitHub Actions workflow locally, and document the homelab workflow. | Verified | Installed `act` 0.2.89 with Homebrew and verified Docker/Colima access. The first local workflow run exposed that `pnpm/action-setup@v4` could not infer a pnpm version, so `package.json` now declares `packageManager: pnpm@11.9.0`. A later `act` run exposed that `.agents/scripts/verify-package-covers.mjs` only probed macOS font paths, so the verifier now also accepts common Linux font paths used by Ubuntu/act images. Validation passed: `node --check .agents/scripts/verify-package-covers.mjs`, `CI=true pnpm run verify:package`, and `DOCKER_HOST=unix:///Volumes/NVMe/docker/colima/default/docker.sock act push -W .github/workflows/ci.yml -j check -P ubuntu-latest=catthehacker/ubuntu:act-latest --container-architecture linux/arm64 --container-daemon-socket /Volumes/NVMe/docker/colima/default/docker.sock`. Homelab was updated separately to track `act` in `vars/install/desktop.yml` and document the Colima socket command plus optional `basecamp/gh-signoff` usage in `docs/osx.md`. | `act` cache restore/save warnings are expected locally because it is not GitHub's hosted cache service. `gh-signoff` is complementary commit-status policy, not a workflow runner and not enabled by this package. |
| PP-196 | Papyrus, `papyrus-template`, and `marcelofelix` should each have adequate Makefile, pre-commit, and GitHub Actions coverage. | Verified | Papyrus now has `make pre`, `.pre-commit-config.yaml`, and CI coverage for check, build, package/docs verifiers, and `verify:release`. `marcelofelix` now has `make pre`, `.pre-commit-config.yaml`, `.github/workflows/ci.yml`, `packageManager: pnpm@11.9.0`, and a non-local Papyrus dependency on `github:marcelofpfelix/papyrus#0.2.0`. `papyrus-template` now has a Makefile, local pre-commit config, CI workflow, lockfile, `packageManager: pnpm@11.9.0`, and the same GitHub Papyrus dependency until npm publish is complete. Validation passed on 2026-07-11: Papyrus `CI=true make pre`, Marcelofelix `CI=true make pre` and `CI=true make build`, and Papyrus-template `CI=true make pre` and `CI=true make build`. Homelab already had its own Makefile, `.pre-commit-config.yaml`, and CI workflow and was left unchanged. | After `astro-papyrus@0.2.0` is published to npm, switch the consumer dependencies from the GitHub branch to `^0.2.0` and refresh their lockfiles. |
| PP-197 | Move duplicated consumer implementation from `marcelofelix` and `papyrus-template` into Papyrus, then publish the package without losing simple template cloning. | Verified | Added `.agents/consumer-dry-npm-publish.md` with the DRY/KISS migration plan, npm publishing task, consumer-boundary checklist, and current asset leak notes. `astro-papyrus@0.2.2` is now published to npm, and the consumer branches have source imports/package names updated to `astro-papyrus`. Papyrus owns shared markdown config (`astro-papyrus/markdown`), callout icons, sitemap filtering (`astro-papyrus/sitemap`), shared content schema fields, an injected `/llms.txt` route, and inlined Shiki code-block copy/collapse icons, so consumers no longer need to carry `public/icons/code.svg`. `marcelofelix/dev` was reduced to rely on Papyrus injected routes and local TOML/content/assets. `papyrus-template/0.2.2` uses the same Papyrus markdown and sitemap helpers. Papyrus commit `7c6fff3` was pushed to `origin/0.2.2`; `papyrus-template/0.2.2` commit `b58f84b` and `marcelofelix/dev` commit `bcd4c64` refresh `package.json` and `pnpm-lock.yaml` to `github:marcelofpfelix/papyrus#7c6fff3`, resolving package metadata as `astro-papyrus`. Validation passed on 2026-08-15: Papyrus `CI=true pnpm run verify:public-content`, `CI=true pnpm run verify:ai`, `CI=true pnpm run audit:status`, `CI=true pnpm run verify:config`, `CI=true pnpm run verify:markdown`, `CI=true pnpm run verify:assets`, `CI=true pnpm run verify:pure-parity`, `CI=true pnpm exec astro check`, and `CI=true pnpm run build`; `papyrus-template` `CI=true pnpm run precommit` and `CI=true pnpm run build`; `marcelofelix` `CI=true pnpm run precommit` and `CI=true pnpm run build`. | Keep simple template cloning by leaving consumers limited to config/content/data/assets. After the npm minimum-release-age gate passes, switch consumers to `astro-papyrus@^0.2.2` or explicitly keep the GitHub commit ref. |
| PP-198 | Cover generation should not create or keep orphan post cover files when post frontmatter does not reference them. | Verified | `scripts/create-cover-svg.mjs` now skips Markdown cover generation unless the target public path matches `cover` frontmatter, removes a pre-existing orphan target, and prints the skip reason. `CI=true pnpm run verify:package` passed on 2026-08-14 and now checks both the configured-cover generation path and the no-cover orphan removal path. | Keep generated post covers frontmatter-owned; local one-off consumer scripts should either set `cover` or not create files. |
| PP-198A | Add configurable `security.txt` support for Papyrus sites. | Verified | Added `[security_txt]` parsing with contact, preferred languages, canonical, policy, acknowledgments, expires, encryption, and hiring fields. Papyrus injects `/.well-known/security.txt` and `/security.txt` only when contacts are configured, avoiding placeholder static files on Cloudflare/Pages. The site-config and feature docs explain the behavior. `CI=true pnpm run verify:config`, `CI=true pnpm exec astro check`, and `CI=true pnpm run build` passed on 2026-08-14; the built demo no longer emits `dist/security.txt` when unconfigured. | Consumers must add their own `contact` value before scanners will see security.txt. Do not hardcode personal contacts in the package. |
| PP-199 | Add configurable Papyrus image effects for theme-toned images: tritone/palette mapping and optional dither mode inspired by `mitsuhiko/dark`. | Verified | Reviewed upstream `mitsuhiko/dark` `blog/static/app.js`: it uses WebGL over `/static/waves.mp4` plus fallback image, luminance thresholding, Atkinson-like/Bayer/noise dithering, theme-aware ink colors, alpha/background fallback, and a separate `.dithered-image` runtime. Papyrus supports `tritone` as a CSS-only runtime effect, `dither` as one dark/light build-time alpha mask, and `dithernoise` as four dark/light build-time alpha mask frames colored by runtime theme variables. Generated image-effect files now live in versioned per-source folders under `public/generated/{effect}/v*/...`. Post/list covers can opt in with `cover_effect`; profile avatars can opt in with `avatar_effect` or `[profile.images].effect`. Content videos can opt into inherited runtime dither with `class="papyrus-video-effect-dither"`; the runtime uses WebGL, a poster/normal-video fallback, visibility pausing, reduced-motion support, and theme CSS variables. | Keep image masks build-time and deterministic. Keep video dither explicit and runtime-only; defer 2D fallback, cover-video frontmatter, and Bayer/noise mode selection until there is a concrete use case. |
| PP-200 | Review Minimal Mistakes as a reference and decide which features Papyrus should copy, adapt, or skip. | Deferred | Added `.agents/minimal-mistakes-comparison.md` after reviewing current upstream README/docs for notable features, layouts, headers, sidebars, galleries, feature rows, search, comments, analytics, SEO, and localization. The 2026 follow-up also compared Astro, Hugo, Eleventy, analytics market-share, and Reddit-style community summaries; the useful ideas are explicitly documented as Maybe backlog items, not approved implementation work. | Revisit only when one of the Maybe items becomes a concrete request: header/teaser media, breadcrumbs, related posts, gallery/figure helpers, responsive embeds, per-page classes, custom head/footer hooks, opt-in analytics, broader webmaster verification, or optional sidebar blocks. |
| PP-201 | Add optional source-tone overrides for `duotone` and `tritone` images. | Deferred | Current CSS-only `duotone` and `tritone` effects use theme variables and do not analyze source-image luminance. This is usually fine, but very dark or very light source images may need a manual hint so the mapping can boost/lighten or deepen the result when the site switches theme. Do not add build-time auto-detection until a real image proves it is needed; auto-detection adds image analysis complexity, remote-image edge cases, and crop/object-position ambiguity. | Future implementation should add `tone = "auto" | "dark" | "light"` only for CSS tone effects, exposed as `cover_tone`/`coverTone` for post covers and `[profile.images].tone` or `avatar_tone` for profile images. Default must remain `auto` and preserve current output. The implementation should set a shared class/data attribute consumed by CSS, add one demo image that shows why the override exists, document the option, and update config/content/profile verifiers. |
| PP-202 | Low priority: allow an optional hover transition from `duotone` to `tritone`. | Deferred | Marcelo asked for a task only, not implementation. This should be an explicit opt-in because hover does not translate cleanly to touch devices and can make image previews feel unstable if enabled globally. | Future implementation should add an option such as `hover_effect = "tritone"` or `cover_hover_effect = "tritone"` for CSS-only image effects, keep the default disabled, respect `prefers-reduced-motion`, avoid layout shift, and define the mobile/touch fallback before adding docs and visual tests. |
| PP-203 | Add a theme-aware SVG authoring path so selected SVGs can change colors with the active theme. | Verified | Documented the public authoring contract in the Markdown guide and theme spec: inline SVGs can use `currentColor` or Papyrus CSS variables, SVGs loaded through `<img>` must carry their own `var(--papyrus-*, fallback)` values, and arbitrary uploaded SVGs are not automatically recolored. The Markdown guide now includes a rendered inline SVG example using `currentColor`, `--papyrus-panel`, and `--papyrus-accent`. Asset verification checks that generated/token-owned SVGs such as the logo, package-shape cover, and markdown cover use `currentColor` or Papyrus theme variables. `CI=true pnpm run verify:markdown` and `CI=true pnpm run verify:assets` passed on 2026-08-14. | Keep normal uploaded SVG files unchanged by default. Add a dedicated helper/component only if repeated real examples show it removes meaningful duplication. |
| PP-204 | Add automatic GitHub release/versioning and npm publish automation for Papyrus. | Verified | Added `.github/workflows/release.yml`: Release Please opens release PRs on `main`, and published GitHub releases run install, `pnpm run verify:release`, build, then `pnpm publish --access public --provenance` with `id-token: write` for npm Trusted Publishing/OIDC. Release docs now explain the workflow and required npm trusted publishing setup. `CI=true pnpm exec astro check`, `CI=true pnpm run build`, and network-enabled `CI=true pnpm run verify:release` passed on 2026-08-14. | npm Trusted Publishing must still be configured in npm for this exact GitHub workflow before the publish job can publish without a token. |
| PP-205 | Update Papyrus installation docs to explain npm installs and the current GitHub dependency method. | Verified | README and the install guide now show `astro-papyrus@^0.2.2` as the normal npm path and `github:marcelofpfelix/papyrus#0.2.2` as the pinned GitHub testing path, with guidance to prefer tags or commit SHAs over floating branches for real sites. The install guide also shows `astro-papyrus/astro`, `astro-papyrus/content`, and `astro-papyrus/config` imports. `CI=true pnpm run verify:config` passed on 2026-08-14. | Keep `papyrus-template` aligned with this docs stance when consumer lockfiles are refreshed. |
| PP-206 | Add an opt-in analytics strategy that fits static themes without making analytics a default dependency. | Verified | Added disabled-by-default `[analytics]` config with provider support for `ga4`, `plausible`, `umami`, `goatcounter`, and `custom`, plus `include_in_dev` to keep analytics production-only by default. `PapyrusBaseLayout` renders the selected provider only when enabled. Site-config docs recommend privacy-first examples and document Lighthouse/privacy tradeoffs. `CI=true pnpm run verify:config`, `CI=true pnpm exec astro check`, and `CI=true pnpm run build` passed on 2026-08-14. | Keep analytics off by default. Add provider-specific tests only if a real consuming site enables one. |
| PP-207 | Broaden site verification beyond only Google Search Console. | Verified | Kept the Google shorthand and added generic `[[verification_meta]]` plus provider fields for Bing/msvalidate, Yandex, Pinterest, and Facebook. `PapyrusBaseLayout` dedupes and emits configured verification meta tags. Site-config docs show both shorthand and generic forms. `CI=true pnpm run verify:config`, `CI=true pnpm exec astro check`, and `CI=true pnpm run build` passed on 2026-08-14. | Add another provider field only when it maps cleanly to a meta name; otherwise use `[[verification_meta]]`. |
| PP-208 | Add a constrained custom head/footer extension point for site-owned snippets. | Verified | Added typed `[head]` entries for meta, link, and external script tags, rendered by `PapyrusBaseLayout` without accepting raw HTML strings. Added typed `[[footer]]` config for normal footer text links, while `[[social]]` remains the constrained footer icon-link path. `PapyrusBaseLayout` now uses configured footer links unless a page passes explicit `footerLinks`. Site-config docs and the public theme spec explain when to use `[head]`, `[[footer]]`, and `[[social]]`, and explicitly reject arbitrary footer HTML/scripts. `CI=true pnpm run verify:config`, `CI=true pnpm exec astro check`, `CI=true pnpm run verify:public-content`, `CI=true pnpm run verify:ai`, and `CI=true pnpm run build` passed on 2026-08-15. | If a future provider needs a footer widget, design a typed footer-widget shape instead of raw injection. |
| PP-209 | Add constrained configurable Markdown prose styling presets. | Verified | Added typed `[markdown]` config defaults and TOML parsing for `link_style`, `heading_style`, `marker_style`, `blockquote_style`, `table_style`, `table_header_style`, and `inline_code_style`. `PapyrusBaseLayout` now emits stable root data attributes, and `papyrus.css` maps those presets to theme tokens for prose links, headings, markers, blockquotes, tables, table headers, and inline code. The Papyrus demo config, site-config docs, and public theme spec document the supported values and non-goals. `CI=true pnpm run verify:config`, `CI=true pnpm exec astro check`, and `CI=true pnpm run build` passed on 2026-08-14. | Keep the style surface constrained to named presets and theme tokens. Do not add custom hex colors, per-heading-level controls, separate bullet vs ordered-list controls, detailed table border color knobs, or radius/padding/thickness knobs until a real user need justifies them. |


## Explicit example request coverage

This section maps Marcelo's explicit example checklist to the audit rows above.
When one of these bullets changes, update the referenced row instead of marking a
large batch as done.

| Example request | Tracking rows | Current review note |
| --- | --- | --- |
| Posts in folders/subfolders get automatic tags. | PP-080, PP-082A | Folder-tag helpers are verified, and `/posts/` desktop/mobile tag placement is browser-checked through PP-082A. |
| Auto-create docs/indexes from file structure; first folder file names the collection. | PP-081, PP-081A, PP-081B | Metadata behavior is verified in scripts, `public/demo/content-structure.md` is checked against generator output, and `/docs/content-structure/` plus the docs index are browser-verified as rendered nested UI. |
| Script creates SVG logo, using text color and primary hover color. | PP-064, PP-064A, PP-073, PP-073A, PP-153 | Generated Twinkling SVG/logo/favicon consistency, terminal override generation, and browser hover behavior are verified. |
| Terminal cursor blinking should be CSS/SVG and efficient. | PP-063, PP-072, PP-153 | CSS/SVG blink remains available for terminal overrides such as marcelofelix's `~ $`; the package default Twinkling logo has no cursor. |
| Move light/dark/theme/font controls to footer, using Pure-like icons/dropdowns. | PP-062, PP-062A, PP-062B, PP-062C, PP-068 | Footer controls are browser-checked for Pure-like three-icon mode cycling, icon-only square button geometry, dropdown positioning, close behavior, and active profile/font states. |
| Mobile menu should compact to hamburger, showing search and hamburger only. | PP-065, PP-128 | Mobile header behavior is verified at 390px; screenshots/touch QA remain partial. |
| Header hides on scroll down and shows when scrolling up, with normal background. | PP-066 | Browser behavior is verified. |
| Remove roadmap from public pages; keep repo-only roadmap/status and dummy examples. | PP-099, PP-099A | Verified: audit/status markdown files remain in `docs/`, and the public demo routes are absent. |
| Copy buttons should not say failed after success. | PP-048, PP-059A | Success paths and rejected-clipboard fallback states are browser-verified. |
| Social links and RSS should live in footer; RSS tab removed. | PP-074, PP-075, PP-076, PP-078 | Footer powered-by, RSS, social links, and transparent icon backgrounds are browser-verified. |
| Remove home tab; logo goes home. | PP-063, PP-107 | Package demo behavior is verified by PP-063; final consuming-site proof remains tracked under PP-107. |
| Obsidian callouts use upstream `rehype-callouts` behavior with no local duplicate icons. | PP-041, PP-054, PP-055 | Colors/icons/collapsible callouts are browser-verified; custom GitHub-alert code was removed. |
| High-priority code blocks: Pure/Shiki look, copy icon, title, and diff fence. | PP-001, PP-040, PP-048, PP-050, PP-051, PP-148 | Current behavior is copied from upstream Pure's documented Shiki transformer setup: line numbers, language labels, titles, copy icon/button, diff notation, highlight notation, and collapse. |
| Pure-style `more about me` and `more posts`, no search link. | PP-059B, PP-059E | Behavior and no-border styling are verified in the browser. |
| Powered-by note like Pure. | PP-074, PP-075 | Browser checks verify one powered-by icon, exact text, and `papyrus`/`Astro` links. |
| Tags in posts page with tag icon, no label text, `#tag`, mobile and desktop. | PP-082, PP-082A, PP-088, PP-089 | Verified for the package demo by `make verify-browser`: `/posts/` now has desktop and mobile tag checks, no tags page, and tag links go to search filters. |
| Post TOC should be optional/collapsed and section controls should be easy to access. | PP-059G, PP-081C | Collapsed post TOC and docs section popup behavior are browser-verified. |
| Pure-style back-to-top popup button should appear on long posts. | PP-059F | Browser checks verify visibility after scroll and click-to-top behavior. |
| Posts index should follow Pure where practical. | PP-088A, PP-090 | Verified for practical Pure-pattern adaptation: source comparison covers Pure `PostPreview` shape and papyrus `/posts/` behavior; exact visual/pixel parity is not claimed. |
| Rename archive to Timeline and link with clock icon. | PP-087, PP-088 | Verified for package-demo UI: `/posts/` links to `/posts/timeline/` with a clock icon and no global Archive tab. |
| Post metadata line shows created date, updated date when different, read time, tags, icons. | PP-059C, PP-098 | Created date, updated-date edge case, read time, tag links, and icons are browser-verified; git-hook date automation remains separate under PP-098. |
| Comments/views stats at bottom and real comment-system decision. | PP-059D, PP-129 | Default decision is verified as Giscus; live configured comments/stat endpoint rendering remains partial under PP-059D. |
| Add task-list markdown support. | PP-042 | Verified in browser. |
| Community plugins are important, Starlight-like. | PP-123, PP-123A, PP-123B, PP-124, PP-146, PP-150, PP-151 | Verified for papyrus's lightweight plugin contract, example external package fixture, lifecycle hooks, Starlight idea comparison, Starlight ecosystem comparison notes, Pure pass-through exports, and split optional runtime modules. Full Starlight-like plugin architecture remains a maybe/deferred task under PP-151. |
| Generate favicon/mobile assets from logo with a script. | PP-073, PP-073A | SVG logo/favicon, PNG mobile icons, Apple touch icon link, and manifest entries are generated and verified. |
| Create docs for the theme like Astro/Pure docs. | PP-013, PP-015, PP-016, PP-081B | Docs and the rendered content index exist, but completeness/demo coverage is still partial. |
| Profile/CV should reread jekyllcv and bandonga/cv, mobile-first, Pure components, projects, GitHub cards, same data for A4, TOML, JSON/Markdown export, font/color/template controls. | PP-100 through PP-110 | Source re-read, data normalization, exports, project/GitHub card demos, shared A4 data, template controls, and reusable component coverage are verified. Exact old jekyllcv visual/PDF parity, mobile-first parity, and final consuming-site profile integration remain partial. |

## External request coverage

These requests appeared in the same conversation history but are not
papyrus implementation rows. They stay visible here so they are not mistaken
for missing package work or quietly marked complete in the wrong repository.

| Request area | Owning repo / scope | Current papyrus decision |
| --- | --- | --- |
| Create `gt`, `tmx`, `blog`, `notes`, `profile`, `marcelof`, and `astrocv` repos with `gwt`. | Repo bootstrap / multiple worktrees | Out of scope for papyrus; do not track as a package feature. |
| Blog article based on `website.md`, AI stack post, and blog URL choice (`blog.marcelofelix.com` vs `marcelofelix.com/blog`). | Consuming blog content repo | Out of scope for papyrus unless it becomes demo content. |
| Consuming blog starts from AstroPapyrus and mentions Cactus, Astrofy, Astroplate, AstroWind alternatives. | Consuming blog repo | Historical direction; papyrus later became a separate reusable package. |
| Notes site should use Astro Starlight. | `notes` repo | Out of scope for papyrus; Starlight remains a reference only. |
| Dotfiles Cloudflare token script should use `gopass homelab/cloudflare` and live in dotfiles/`~/bin`. | Dotfiles / local shell tooling | Out of scope for papyrus; package docs may mention external token wrappers only. |
| Cloudflare Pages deploy authorization and one-time setup. | Deployment docs / Cloudflare account state | Docs are verified under PP-140 and PP-145; actual deploy remains deferred under PP-032. |
| Blog/profile consuming site should install `papyrus` and keep content/config minimal. | Consuming `marcelofelix` site | Tracked as PP-012 and PP-016 until integration work starts in that repo. |

## Current verification commands

Last broad verification in this audit:

```sh
CI=true pnpm run verify:public-content
CI=true pnpm run verify:ai
CI=true pnpm run audit:status
CI=true pnpm run verify:config
CI=true pnpm run verify:markdown
CI=true pnpm run verify:assets
CI=true pnpm run verify:pure-parity
CI=true pnpm exec astro check
CI=true pnpm run build
```

Additional link audit:

- Built artifact link checker passed across HTML, JSON, XML, and TXT files.
- `make dev` starts on `localhost:4326` after stopping old listeners.
- `make serve` returns 200 for LAN URLs under `http://192.168.1.102:4326/`.
- `make verify-package` checks package exports, bin targets, Pure dependency
  boundary, and generated cover SVG structure.
- `make verify-markdown` checks markdown demo source coverage and markdown
  helper wiring.
- `make verify-browser` checks browser behavior for post-card/tag/side-link
  borders, post side links, copy/share, Obsidian callouts, blockquotes, task lists,
  borderless tables, image/SVG zoom, Mermaid SVG hydration including fenced
  markdown and theme re-rendering, footer theme controls, header scroll state,
  tags/meta, and CV A4 routes.

These checks do not prove exact Pure visual parity, mobile screenshot quality,
non-Mermaid diagram rendering, or old CV PDF/visual parity.
