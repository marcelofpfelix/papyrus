# Pure parity checklist

This file is the working checklist for requests that say papyrus should copy,
adapt, or prioritize behavior that already exists in Pure. It does not mean every
item is visually identical today. It records the source to inspect first, the
papyrus owner, the current state, and the next evidence needed before a row in
`.agents/request-audit.md` can be marked fully verified.

When implementing or polishing one of these areas, check Pure first. If the Pure
code fits the package boundary, adapt the component shape or behavior in
papyrus. If it does not fit, record the reason before building a local variant.

| Feature area | Pure reference to inspect first | papyrus owner | Current status | Next evidence |
| --- | --- | --- | --- | --- |
| Pure public exports | `astro-pure` package exports: `./user`, `./advanced`, `./components/pages`, `./components/basic`, `./utils`, `./libs` | `package.json`, `src/pure/*` pass-through modules, `examples/pure-pass-through/` | Supported with a narrow runtime bridge. `astro-papyrus` exposes `astro-papyrus/pure`, `astro-papyrus/pure/user`, `astro-papyrus/pure/advanced`, `astro-papyrus/pure/pages`, `astro-papyrus/pure/basic`, `astro-papyrus/pure/utils`, and `astro-papyrus/pure/libs` through pass-through exports. `examples/pure-pass-through/` imports representative public Pure components from advanced, basic, pages, and user groups. The Papyrus integration now maps Papyrus site config into a conservative Pure-compatible `virtual:config` shape with title, description, site, locale, header, footer, author, content.share, integ.quote, and integ.mediumZoom. | Full Pure-site behavior is not claimed. If a Pure component needs additional config fields, extend the bridge only with stable public Pure expectations or narrow the documented pass-through surface. |
| Theme controls | `node_modules/astro-pure/components/basic/Header.astro` and `node_modules/astro-pure/utils/theme.ts` | `PapyrusFooter.astro`, `PapyrusThemeProvider.astro`, `papyrus.css` | Theme controls are adapted from Pure where compatible: `verify-pure-parity` checks Pure's storage-backed three-icon mode button and theme utility pattern, then checks papyrus's footer three-icon mode button, active icon CSS, profile/font dropdown markup, storage-backed mode cycle, aria-label sync, and dropdown close behavior. `verify-browser` checks the rendered mode cycle and theme/font dropdown behavior. Exact Pure visual parity remains open. | Screenshot or DOM/CSS comparison for exact Pure-like spacing, icon grouping, and dropdown appearance. |
| Code blocks | Upstream Pure app Shiki setup in `/tmp/astro-theme-pure/astro.config.ts`, `/tmp/astro-theme-pure/src/plugins/shiki-custom-transformers.ts`, `/tmp/astro-theme-pure/src/plugins/shiki-official/transformers.ts`, and `/tmp/astro-theme-pure/src/assets/styles/global.css` | `src/markdown/config.mjs`, `src/shiki/*`, `papyrus.css`, `/collections/docs/code-demo/` | Pure parity is copied from the upstream Pure docs/source rather than from a package component export. `papyrusMarkdown()` uses Astro Shiki `theme: "css-variables"` plus Pure's transformer order: diff, highlight, remove-escape, wrapper update, title, language, copy button, and collapse. `papyrus.css` copies the Pure line-number/title/language/copy/diff/highlight/collapse structure while mapping colors to Papyrus tokens and preserving the verified compact `14px` / `21px` code typography. Code-block copy/collapse icons are inline `currentColor` SVGs, so consuming sites do not need `/icons/code.svg`. | Exact pixel comparison against a Pure screenshot can still be added if visual parity becomes stricter than behavior parity. |
| Markdown integration plugins | Pure integration internals in `node_modules/astro-pure/index.ts` and `node_modules/astro-pure/plugins/*` | `astro.config.mjs`, local markdown helpers, package docs | Verified boundary. Pure wires `remarkAddZoomable`, `remarkReadingTime`, `rehypeExternalLinks`, `rehypeTable`, and `rehypeImageCaption` inside the `astro-pure` integration, but Pure markdown plugins are integration-private package files and are not deep-imported by `papyrus`. `verify-pure-parity` checks that Pure has not published stable plugin subpath exports yet. | If Pure publishes public plugin exports, replace compatible local helpers with stable imports. Until then, keep local helpers only for Papyrus-specific behavior and document the boundary. |
| Timeline/profile | `node_modules/astro-pure/components/user/Timeline.astro`, `node_modules/astro-pure/components/advanced/GithubCard.astro`, and Pure layout primitives | `PapyrusTimeline.astro`, `PapyrusGithubCard.astro`, CV/profile components, `/profile/`, `/profile/print/`, `/profile/ast/`, `/posts/cv-profile/` | Partial. Behavior is adapted and browser-verified, and profile action controls now use the shared `PapyrusCvActions` package component instead of copied route markup. `PapyrusTimeline` and `PapyrusGithubCard` remain local implementations rather than wrappers around Pure components. | Decide direct wrapper vs justified local implementation; prefer importing Pure where its props and styling boundary fit. |
| GitHub repository preview | Pure `astro-pure/advanced` `GithubCard` | `PapyrusGithubCard.astro`, `PapyrusProjectList.astro`, `/projects/` | Resolved under `PP-215`. Keep `PapyrusGithubCard` as the deterministic/static variant because profile/about cards need local descriptions and no browser GitHub API request. Pure's live card remains available through `astro-papyrus/pure/advanced`. | Revisit only if Pure exposes a static-data prop that removes the network and UnoCSS assumptions. |
| Link preview | Pure `astro-pure/advanced` `LinkPreview` and `plugins/link-preview.ts` | `PapyrusLinkPreview.astro`, docs/home cards | Resolved under `PP-215`. Keep `PapyrusLinkPreview` as the deterministic, base-path-aware variant. Pure's build-time remote OpenGraph card remains available through `astro-papyrus/pure/advanced`. | Revisit only if a package-owned route needs remote OpenGraph fetching. |
| Posts index | `node_modules/astro-pure/components/pages/PostPreview.astro` | `PapyrusPostList.astro`, `/posts/` | Partial. Posts index is adapted from Pure `PostPreview` where practical, but `PapyrusPostList` is a local implementation rather than a wrapper. | Add a wrapper or adapter around Pure `PostPreview` for list/detailed mode, then keep Papyrus-only card/compact variants only if needed. |
| TOC and back-to-top | `node_modules/astro-pure/components/pages/TOC.astro`, `TOCHeading.astro`, `BackToTop.astro` | `PapyrusToc.astro`, `PapyrusPostLayout.astro` | Wrapped and browser-verified. Papyrus keeps its collapsible desktop/mobile shell and collection links, renders article headings with Pure `TOC`, and uses Pure `BackToTop` with Papyrus IDs and token styling. | Keep the wrapper props stable and rerun the focused browser verifier when Pure changes these public components. |
| Markdown styles | AstroPapyrus callouts plus Pure markdown/prose styles | `papyrus.css`, `rehype-callouts`, `remark-mermaid-blocks.mjs`, markdown demo routes | Callouts now follow AstroPapyrus v6.1: `rehype-callouts` with the Obsidian theme CSS. `verify-browser` checks rendered `.callout` variants, upstream SVG icons, and collapsible `<details>` behavior. Blockquotes, task lists, tables, Mermaid, artifact links, and copy/source actions also have browser coverage. | Compare remaining tables, blockquotes, and prose spacing against Pure fixtures. |

## Pure wrapper decisions

These decisions keep `papyrus` thin without making the package depend on
Pure private internals or forcing Pure's full site assumptions into every
consumer.

| Component / behavior | Decision | Reason |
| --- | --- | --- |
| Pure user components | Direct pass-through via `astro-papyrus/pure/user`. | Public Pure export; safe for consumers that want Pure primitives directly. |
| Pure advanced components | Pass-through via `astro-papyrus/pure/advanced`, with representative fixture coverage. | Public Pure export; exposes `GithubCard`, `LinkPreview`, `QRCode`, `MediumZoom`, and related components without copying them. The Papyrus `virtual:config` bridge supplies the common Pure fields these components may read. |
| Pure page components | Pass-through via `astro-papyrus/pure/pages`, with representative fixture coverage. | Public Pure export; consumers can opt into `PostPreview`, `TOC`, `BackToTop`, and page primitives directly. Route, content-shape, and styling assumptions still belong to the consuming site. |
| `PapyrusGithubCard` | Keep the local static/deterministic Papyrus implementation. | It accepts caller-provided descriptions, performs no GitHub API request, and does not require Pure's UnoCSS classes. The explicit live alternative is `GithubCard` from `astro-papyrus/pure/advanced`. |
| `PapyrusLinkPreview` | Keep the local static variant. | It is deterministic and base-path-aware. The explicit build-time remote OpenGraph alternative is `LinkPreview` from `astro-papyrus/pure/advanced`. |
| `PapyrusTimeline` | Keep local Papyrus implementation for now. | The props intentionally mirror Pure's simple event model while using Papyrus tokens and CV/profile layout needs. Pure's `Timeline` remains available through `astro-papyrus/pure/user`. |
| `PapyrusPostList` | Keep local adapter for package posts pages. | Pure `PostPreview` expects Pure collection shapes and `/blog/${id}` routing. Papyrus supports list, compact, and cards views, pinned posts, `/posts/`, and tag search links. Pure page components remain available through `astro-papyrus/pure/pages`. |
| `PapyrusToc` | Wrap Pure `TOC` inside the Papyrus collapsible shell. | Pure owns article hierarchy and progress behavior; Papyrus retains mobile/desktop disclosure behavior and collection section links. |
| Back-to-top | Wrap Pure `BackToTop` in `PapyrusBackToTopRuntime`. | Papyrus supplies stable header/content IDs and token CSS while Pure owns scroll percentage and top navigation. |
| Share/source actions, Mermaid/artifacts, image zoom | Use split Papyrus runtime components. | These are Papyrus-specific enhancements around source copy, share copy, theme-aware Mermaid rendering, artifact cards, and zoom overlay behavior. Code blocks use the Pure Shiki transformer behavior copied from upstream Pure. |

Checklist status rules:

- `Functional` means papyrus has a working local implementation and a focused verifier.
- `Pure-like` means the implementation was compared against a Pure source file or rendered Pure fixture.
- `Verified parity` requires repeatable browser evidence or a documented screenshot/PDF comparison.
- A broad build, link check, or script existence check is not enough to close a visual parity row.
