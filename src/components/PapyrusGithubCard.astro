---
import { renderGithubCard } from "../utils/github-card.mjs";

interface Props {
  repo: string;
  description?: string;
}

const { repo, description = "" } = Astro.props;
const markup = renderGithubCard(repo, description);
---

<Fragment set:html={markup} />
